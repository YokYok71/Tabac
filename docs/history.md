# Findings — what broke, how it was measured, what it cost

*Split out of `CLAUDE.md`. The gates read this file too — see `DOC_FILES` in `scripts/doc-check.cjs`.*
## Scale — four fixes, and the two halves of the list freeze

**AN ENTRY ANIMATION COULD BE SCHEDULED MINUTES OUT.** Every list computes its delay from the row index (`100 + idx * 50`), unbounded: MEASURED on a 5000-session journal the last card's timer fired at **250 seconds**, each row its own `setTimeout` + `setState` over a **165 118**-node tree — at 4x CPU throttle, 288 further long tasks averaging 122 ms, still going when observation stopped. `ENTER_MAX_DELAY_MS` (700) caps the DELAY **inside `useEnter`**, and that placement is the point: capping the index at each call site would cover the six current lists and no future one, whereas the rule — *no entry animation may begin later than this* — belongs to the animation. Nothing visible changes; past ~12 rows the stagger is off screen.

**THE LARGER HALF — every row in the DOM — IS NOW BOUNDED, and this paragraph used to say it was not fixed.** It read *"DISCLOSED, NOT FIXED: the lists are not windowed … 165 118 nodes for that journal, 201 029 for a 20 000-row catalogue"*, and those figures were close to the ones a fresh measurement produced (185 654 and 200 613). The flat branches render a growing prefix now — see `useProgressiveList` in the structure tree for the before/after table and for what is still NOT windowing. The correction is left in place rather than deleted because a note recording a deferral has to be closed by whoever resolves it: a stale to-do does not merely go useless, it points the next reader backwards.

**`computePipeUsageProfile` WAS QUADRATIC — the twin of a defect already fixed once.** It rebuilds its tobacco→category map AND rescans every session for EACH pipe, so calling it per pipe is O(pipes x sessions): **61.8 ms** at 200 x 5000, against **0.96 ms** for `computePipeRest` and **2.23 ms** for `computePipeMaintenanceReminders` on identical inputs — its two siblings in the same component, both of which hoist their index. `PipesListView`'s memo depends on all three `liveData` arrays, so it re-paid that **on every save, from every screen** (the view is mounted even while it returns null). `buildPipeCategoryIndex(sessions, tobaccos)` is the optional precomputed index, exactly the repair `pipeSessionsSinceMaint` was given after a consolidation dropped ITS hoist: one implementation, and single-pipe callers pass nothing and are unchanged.

**THE JOURNAL SORT PUT A LINEAR `.find` INSIDE THE COMPARATOR.** At 5000 sessions x 300 tobaccos, sorting by date is 6.2 ms and sorting by tobacco was **257 ms — 41x** — doubling on EITHER axis. `sortBy` is local state, but the view never unmounts, so once picked it re-paid on every save from any screen. A label `Map` is built once outside the comparator; `restMap` in the same file already had the shape. `tobOf`/`pipeOf` stay for the RENDER path, where they run once per visible row.

**THE CATALOGUE'S OWNED/WISHED SETS RE-RESOLVED THE WHOLE CELLAR ON EVERY SAVE.** Both memos were keyed on `data`, which changes identity on every save, while `db` is set on the first visit to that page and never cleared (the view self-gates rather than unmounting) — so after one visit this ran from the Home screen for the rest of the session. Cost is per MISS, and a miss falls through the full fuzzy ladder: **1 309 ms** at 300 tobaccos against a 20 000-blend catalogue, **145 ms** even against a 1 594-blend one. Keyed on the COLLECTION now (`tobList` / `wishList` hoisted so the dep is exact rather than omitted — the warning budget stays at 7).

**MEASURED AND FOUND FINE, so the next reader does not re-chase them:** every pure aggregator is linear and cheap at 300/200/100/5000 (`computeChartStats` 3.9 ms, `computeTasteProfile` 9.9, `computeAgingSweetSpot` 9.0, `computeAromaProfile` 8.6, `buildTobaccoAromaIndex` 6.3, `computeCostPerSession` 1.6, `computeWatchlist` 0.7, `duplicateCount` 1.4); `liveData` is a shallow filter+map, not a deep copy; `migrateData` on a 2.77 MB cellar is 36 ms, load-path only. **The engine layer is not the problem.** **CORRIGÉ EN PLACE, ET LA PÉREMPTION EST L'ENTRÉE.** Ce paragraphe se terminait par « *Still open and disclosed:* `appCtx` … the photo effect re-reads every `local-photo-*` from IndexedDB on every save (~63 ms for 300 covers) … and the catalogue search filters 20 000 rows per keystroke undebounced (83-134 ms each) » — **deux des trois étaient déjà réparés quand on est revenu les lire.** Le lecteur de photos saute désormais toute clé que la carte porte déjà (`imgLocalRef`, 0 lecture par sauvegarde contre 600), et la recherche du catalogue est débouncée à `CATALOG_SEARCH_DEBOUNCE_MS` (160 ms) — les deux ont leurs propres entrées plus bas dans ce fichier, écrites par la personne qui a fait le travail et jamais reportées ici. **C'est la panne que ce fichier nomme ailleurs : une note qui enregistre un report doit être refermée par celui qui la résout, sinon elle ne devient pas seulement inutile, elle pointe le lecteur suivant en arrière.** Trouvée en répondant à « il reste quoi ? » et en VÉRIFIANT chaque ligne au lieu de la réciter. **Ce qui reste vraiment ouvert est UN seul point, et c'est une décision, pas un retard** : `appCtx` est un littéral frais à chaque rendu, donc une sauvegarde relance les mémos de toutes les vues montées (150-172 ms mesurés) — le mémoïser demanderait une liste de dépendances écrite à la main sur ~200 clés, c'est-à-dire la classe de fermeture périmée que plusieurs builds ont servi à supprimer, et la mesure a montré que ce n'était PAS la cause du coût qu'on lui attribuait (voir l'entrée `appCtx` ci-dessus).

## Multi-device convergence, as a simulation

**The merge engine has been repaired seven times and every repair came from a hand-written scenario**, so the suite covered the cases somebody thought of. `multiDeviceConvergence.property.test.ts` generates the rest: three devices mounted with the REAL `useTobaccoStore` / `useSessionStore` / `useTrashOps` over a shared save box, ~20 ordinary operations each, then real `stageImport(…, {autoApply: "merge"})` merges — 120 fast-check schedules per property. Six properties, each with a non-vacuity counter asserted after `fc.assert`: `checkAllInvariants` empty after the merge AND after the round trip, no silent loss, no silent duplication without a counter, idempotency, order-independence, and exact lot balance in both directions. **All six held; no production defect.** Probed independently of the author's own five: making an appended lot take a FRESH `uid` instead of the imported one reddens 4 of the 10 cases.

**THE FIDELITY REVERSAL IS THE MOST USEFUL THING IN IT.** The first version reset `monotonicId`'s module counter at every device switch, on the stated grounds that without it the whole `lot-id-unique-global` class is unreachable from one test process. That produced **33 post-merge invariant failures, 24 post-round-trip, 86 `lot-id-unique-global` and 2 `lot-balance-overflow`** — every one MANUFACTURED: the reset restarts the id clock inside a single test millisecond, so the merge's re-stamp landed on an id a LOCAL lot already carried. Remove it and the same 120 schedules yield zero. **A harness knob that makes a rare class "reachable" can instead make it CERTAIN, and the failures it then produces are measurements of the knob.** Same family as the `_langUsable` pseudo-loc mode that was silently dead, and as the seed lessons: what the harness does to reach a state is part of what the run measures.

**The DESIGNED refusals are quantified, not changed.** Over 120 schedules: `trashedSkipped` 15, `identityConflicts` 8, `lotsTrashedSkipped` 3, `sessionsDetached` 2. Each is asserted with a **floor of 1, never a pinned figure** — the exact counts are a property of the generator's weights, so pinning them would fail on a harmless weight change while proving nothing. **The 2/120 does NOT contradict the 78 % the earlier two-device drill measured**: that drill built the state deliberately, this generator reaches it by chance — a difference in what is sampled, which is exactly why the assertion is a floor and not an equality. `unexplained()` subtracts only two documented rules (the uid sits in the target's trash, or the lot's parent row does) and is presence-only, so a row that arrives with wrong CONTENT is caught by the other five properties rather than by it.

**Not simulated, and stated on the file header rather than left to be assumed**: photos / `_imageData`, pipe / accessory / wishlist mutations, the REPLACE path (still the only auto-applying import branch, and it wipes the cellar), the cloud transport, and a wall-clock lot-id collision between two devices. 5.5 s standalone.

## The cloud transport, which was the least-guarded file in the repo

**`useGdriveSync.ts` is 2 800 lines and a mutation pass over it produced TWO mutations — the thinnest coverage of any file here**, and it happens to be the one that decides whether a backup reaches the cloud, which auto file gets overwritten, and whether a second device is told its cellar is stale. 31 cases now cover it and the two hooks beside it (`useTastingSession`, `catalogueCloudStream`). Four were probed at the code, each reddening EXACTLY ONE case with every sibling green — which is the shape that says the rule was genuinely unguarded rather than incidentally covered:

| probe | what it breaks | result |
|---|---|---|
| reverse the catalogue comparator | fetching the OLDEST catalogue back | 1 of 25 red |
| `if (ok \|\| auto)` → `if (ok)` | a failed auto-end leaves a zombie tasting that blocks every update, for ever | 1 of 80 red |
| neuter `quietSaveInProgressRef` | two concurrent quiet saves, the slower overwriting the faster | 1 of 144 red |
| let the auto-save adopt `autoFiles[0]` | this device overwrites ANOTHER device's auto file — the multi-device data-loss path | 1 of 144 red |

**The fourth is the instructive one and it is this repo's oldest failure shape.** `chooseAutoSaveTarget` is a PURE function with its own suite in `gdriveApi.test.ts`, so the rule looked covered — my probe did not touch it. It removed the CALL, and nothing noticed: *the wiring is what rots*, exactly as recorded at `pipeSessionsSinceMaint`, at `reDeductRestoredSessions`, at `findParityGaps`, and at `assertLotInvariants`. A tested helper plus an untested call site is one guarantee, not two.

**On salvaging an agent's work: RUN it, then probe it, and trust neither the prose nor a green dashboard.** These 700 lines came from an agent that stalled before committing, in a worktree. Running them first produced two failures — both in `.claude/worktrees/**`, where vitest's default include had collected the OTHER agents' copies of the same file names, the documented glob hazard rather than a defect in the work. With the exclusions, 249 green. **A test whose mutant cannot be reproduced is worth nothing**, which is why four probes were run rather than the header comments taken at their word; every one held, so the set was kept whole. Note the probe protocol that matters: the backup copy is taken **immediately before** applying the probe, never at the start of a session — a copy taken earlier restores away work done in between (that mistake cost a recovery once already), and `git checkout` is never the way to undo a probe on uncommitted work.

## The gate that was missing: a list renders O(1) rows

**Nothing in this repo measured the DOM.** `size:check` bounds the bundle, `theme:contrast` bounds contrast, `i18n:layout` bounds overflow, and Lighthouse runs against an EMPTY cellar — so three screens sat **thirteen seconds** from any user with all seven gates green. The product answer that suggests itself (forbid a flat list above N rows) was **considered and rejected**: it takes away a control the user chose — flat mode is how you sort by date across months — and a threshold produces a toggle that works at 499 rows and not at 501, which is a control lying about what it does.

**The defect was never the flat list. It was that nothing looked.** CLAUDE.md already carries the lesson twice — *the seed is a screen GATE, and a green run over an empty state is the most reassuring way to miss a screen*. This is the same thing one size up: **a green run over a SMALL cellar is the most reassuring way to miss a freeze.** The two browser checks share a 40-tobacco seed, so neither could ever have seen it.

**`listDomBudget.test.tsx` is that gate, and it is jsdom rather than a third opt-in browser check.** jsdom does not lay out, but it DOES build the DOM, which is the thing that regressed — and it runs on every commit, where an opt-in check goes unrun (`prune` was red for nine releases while this file asserted a clean baseline). The cost is asymmetric in the right direction: with the cap holding, the journal case renders ~60 rows in 0.8 s; with it broken it renders thousands, takes 5.7 s AND fails.

**What is asserted is the SHAPE, not a number.** Each view is rendered twice, at a small N and a large one, and the DOM must not grow with N. A node BUDGET would be a magic number to re-tune every time a card gains a chip; *doubling the data must not double the DOM* is the property itself and cannot be satisfied by tuning. Three things keep it honest: the small render must actually contain rows (a view rendering nothing would satisfy the growth assertion perfectly); a deliberately unbounded list is rendered as a CONTROL, so a `CHROME_SLACK` widened past usefulness is caught; and **both fixture sizes must sit above `PROGRESSIVE_STEP`, which is asserted rather than left to a comment** — the first version had SMALL below the cap, so the small render showed 40 rows and the large one the full 60, and every case failed on a 743-node gap that was the guard WORKING.

Probed by un-wiring the journal and the catalogue: 3 782 → 44 482 nodes and 1 078 → 12 078, each failure naming the view, both figures, and the remedy. **IT COVERED ONE DOOR AND THE SECOND ONE WAS STILL OPEN — the same shape as the note it was written beside.** This file measured only the FLAT branch, on the strength of a sentence saying the grouped branch was « naturally bounded (one collapsed row per brand) » — measured on a NINE-group fixture, so it measured nine headers. With one group EXPANDED: journal 86 → **185 093**, inventory 92 → **140 093**, catalogue 128 → **200 073**; and the catalogue with 20 000 distinct brands, everything collapsed, **140 065**. Five more cases now render each grouped list with a single group holding everything, plus two for the catalogue's headers. Probed by un-capping each in turn: each reddens exactly its own case. **The lesson is about the GATE rather than the code: a budget that measures one branch of a two-branch view certifies half a screen, and reads as certifying the screen.** **A LAST FIXED SLEEP AS AN ARRIVAL PROOF, replaced by a condition.** Every screen proves it arrived by finding a marker, and that proof used to be taken immediately after a FIXED wait — fine for a React-only screen, a timing assumption for `help`, which fetches a ~74 KB document at runtime and then source-slices it per section behind a 1500 ms sleep. Observed once as `steel/light/help: could not be reached` in ONE palette of a six-palette run: a screen reported unreachable for the machine's load rather than for anything about the app, which reads as a navigation defect. `awaitMarker` (shared, bounded) waits for the marker to attach before counting; a marker that never appears costs its budget and then fails exactly as before, so nothing is weakened. **When you add a list view, add it to `VIEWS` there** — the alternative was an ESLint rule, and it was rejected because "an unbounded collection" has no syntactic marker (`no-unscoped-lot-read` works only because it keys on a literal `.lots`), so it would have to guess that a `.map()` renders cards, which is the over-strict trap that gets correct code rewritten.

## Numbers the app printed but never computed

**Seven numeric defects, and what unites them is that each one is a figure the app DISPLAYS or STORES without ever having done the arithmetic the display implies.** Every one was reproduced before being changed.

**(1) A SUM OF TWO GRID VALUES IS NOT A GRID VALUE.** `useSessionStore`'s EDIT path caps the new weight at `avail = safeW(capL.weightG) + ow`, and `0.1 + 2.7` is `2.8000000000000003` in IEEE-754. The result was stored with a bare `String(nw)`, so the journal rendered `2,8000000000000003g` and the CSV export wrote it verbatim. Brute force says **21.2 %** of realistic pairs produce a noisy `avail`. Every other weight in that store already went through `roundWeightToUnit`; the cap was the one arithmetic result that did not. **`_persistSession`'s cap is untouched and must stay so** — `Math.min(w, safeW(lot.weightG))` has no ADDITION, so it can only return a value already on the grid, and a case pins that distinction.

**(2) `Math.round` MEANS 1 g IN GRAM MODE AND 28.35 g IN OUNCE MODE.** A weight is stored in whatever unit the user typed, so `computeYearConsumption`'s `Math.round` reported **0** for a year of three bowls at 0.09 oz and **1 oz** for 20 g actually smoked. `roundAggregateWeight` (lotUtils) is the DISPLAY grid — whole grams, 1 dp ounces — and is deliberately DISTINCT from `roundWeightToUnit`'s DEDUCTION grid: a stored balance needs more precision than a headline total. `trendPct` stays on the RAW sums, because it is a ratio and rounding its inputs would make the percentage disagree with the two numbers printed beside it. **One site was measured and deliberately NOT changed**: `HomeViewV2`'s stock tile, whose ounce branch is already FINER than its gram sibling, so the defect is backwards there — a named case says so, or the next sweep "completes" it.

**(3) THE CSV READER SAW ONE COMMA AND NOTHING ELSE.** `String(v).trim().replace(",", ".")` then `parseFloat` — so `1 234,5`, which is what a French or German spreadsheet emits, imported as **1**, and `1,234.56` as **1.234**: three orders of magnitude of stock, on a module whose stated contract is that an export edited in a spreadsheet round-trips, with the row neither `skipped` nor listed in `issues`. `readNum` now resolves both marks (the LAST one is the decimal) and strips the group separators a spreadsheet really emits — ASCII space, NBSP, narrow NBSP, thin space and the Swiss apostrophe. **A SINGLE comma stays a DECIMAL mark**, deliberately: that is what this parser has always done and what a French spreadsheet writes, so `2,000` reads as 2 and not 2000, and changing it would silently multiply every fr-locale weight by a thousand. Several commas can only be grouping.

**THAT FIX SHIPPED OVER-STRICT AND THE CORRECTION IS THE ENTRY.** The new reader refused anything `parseFloat` would have truncated — right for `1 234,5` → 1, where the answer is silently wrong by three orders of magnitude, and **WRONG for `50g`, where `parseFloat` returns 50, i.e. the correct answer.** Refusing that cell fixes no defect and destroys a weight this parser had always read; and a tin is sold as "50g" or "2oz", so a hand-built file is likely to carry one. The rule that separates the two: **a numeric prefix followed by a run holding NO DIGIT is a unit naming the column the value is already in, so the number is unambiguous.** A leading word (`env. 12` — an approximation) or a second number (`12abc34`) stays refused AND reported, which is the half that was genuinely missing. This module's contract is tolerance everywhere else — accent-insensitive headers, FR+EN aliases, `dd.mm.yyyy`, EN-locale export dates — so a number reader stricter than the rest of the parser would be the odd one out. **The generalisable half: strictness is justified by the WRONGNESS a lenient reader produces, not by leniency itself.** Two pre-existing cases used `50g` as their unreadable fixture and each records the reversal on its own assertion.

**(4) THE SILENCE WAS THE COSTLY HALF, AGAIN.** `skipped` had been computed and read by nobody once before, one importer over; here a truncated `1 234,5` looked exactly like a clean import. The result now carries an EXACT `badNumber` beside the capped detail list, a `"number"` issue kind, and `csv_issues_num` in all six dictionaries, rendered in the panel that already exists.

**(5-6) A DOT DECIMAL IN A COMMA UI.** `hBars` / `donutChart` printed `12.50` on a screen that says `12,50`, and so did the collection report — a document the user prints and files. The two chart helpers take a FORMATTER (their caller already owns their locale-dependent text) and `collectionReport.ts` takes a `formatNumber` OPTION rather than an i18n import, because its language-neutrality is its design premise; `useExportImport` gained `lang` for it, threaded from `App.tsx`. **The wiring is what rots, so it has its own test**: the module's own option-level cases stayed green when the hook was unwired, and only the three new hook cases caught it. Chart series (`catW` / `brandW`) are rounded at the source, so the caption and the bar cannot disagree.

**(7) TWO READERS OF ONE FIELD DISAGREEING.** `collectionReport.num()` used `parseFloat` where `computeStats` uses `Number`, and had no guard against a value large enough to render in exponential notation (`1e+21 €`). It coerces the way `computeStats` does now — deliberately WITHOUT comma-normalising first, because the defect is the disagreement, so the aim is the same answer as everything else, not a better one in one place.

## A test that wrote the repo it was testing

**`dataOnlyRelease.test.ts` snapshotted the real `src/constants.ts` and `public/version.json`, let the script bump them, and restored in `afterEach`.** That holds only while the process survives to the restore AND is the only writer. Neither held. **MEASURED, on a real working tree with uncommitted work in it: `src/constants.ts` came back TRUNCATED and `public/version.json` EMPTY**, and the failure surfaced as *"could not parse APP_BUILD"* — an error about the repo, not about the script, which is what made it take a while to read.

**The mechanism, because it will recur wherever git worktrees are used:** the test resolves `"src/constants.ts"` against `process.cwd()`, which is the PROJECT ROOT for every vitest worker, while the script resolves its own paths against `__dirname`. Vitest's default include is not scoped to `src/`, so three sibling worktrees under `.claude/worktrees/` were collected as extra test files, and their copies of this test read and wrote the MAIN tree's files while their copies of the script wrote their own. Four interleaved writers, one file.

**The fix is the repo's own idiom — injection over ambient.** `applyCataloguePlan` takes `nowIso`, `catalogueSave` takes `nowMs`, `useUserCatalogue` is handed `dlFile`; `bumpDataOnly` now takes an optional **`root`**, defaulting to the repo the script lives in so the CLI and `npm run release:data` are byte-identical in behaviour. The test builds a temp root and asserts **the real files are unchanged**, checked BEFORE the `afterEach` can paper over a write. Probed in both directions: ignoring `root` reddens the isolation case, and moving the DEFAULT reddens six — which is the half that matters, since a `root` that silently became mandatory would make the release write nothing where the release has to happen, and no other case would notice because they all restore what they clobber.

**Two rules for anyone running the suite here.** A test that writes the tree is a hazard whatever its cleanup; prefer a root you can point elsewhere. And **while a worktree exists under `.claude/`, run vitest with `--exclude '.claude/**'`** — otherwise its test files are collected as extra copies with the wrong cwd. The same goes for `eslint .`, which reports a parse error per worktree file (multiple candidate `tsconfigRootDir`s); lint the real paths instead.

## Costs paid on every save

**Three measured costs, and the third is a correction to the finding rather than a fix for it.**

**(1) EVERY COVER PHOTO WAS READ FROM INDEXEDDB TWICE, ON EVERY SAVE.** MEASURED by driving the real `App` over a 300-cover cellar with a counting `imgCache`: **600 `imgCache.get` calls on the cold load and 600 more per save** — a save that touched only `nxT`. **TWO overlapping effects** walked the same collections (one filtered to `local-photo-*` and also covering the session snapshots, one taking every `imageUrl` and covering none), and both wrote back through `imgMap(prev, upd)` — a FRESH object — so `imgLocal` changed identity on every save even when the resolved set was byte-identical, re-rendering every consumer of the map for nothing. The second effect is DELETED (it could only ever contribute a key that is truthy, resolvable from `imgCache` and NOT `local-photo-*`, and `migrateData._clearForeignImageRefs` blanks every `imageUrl` that is not a `local-photo-*` key or a `data:image/…` URI, while a `data:` URI is never an `imgCache` key — pinned by a case, so the deletion cannot quietly start losing photos), and the survivor skips a key the map already holds. **The skip is ONE-DIRECTIONAL and that is the design**: a key NOT in the map is still asked for on every pass, because a miss means the blob is not there YET (an eviction, or an import whose `_imageData` writes land after its `save`), so remembering a miss would make that photo unrecoverable for the session. After: **300 reads on cold load, 0 per save**, and the pre-existing `if (Object.keys(upd).length)` guard now preserves the map's identity. `imgLocalRef` is a latest-ref MIRROR written from an EFFECT, never at render time (`react-hooks/refs` is error-level here) — and it is not one render stale, because React runs a commit's effects in DECLARATION order and the mirror is declared far above the resolver.

**(2) THE CATALOGUE FILTERED EVERY ROW ON EVERY KEYSTROKE.** MEASURED: 6-13 ms per key at 1594 rows, **66-148 ms at 20 000** (`MAX_CATALOGUE_ROWS`), the cost rising with query length because a longer token falls through to the Levenshtein fallback; a phone is several times slower again. Typing "capstan blue" paid that twelve times, each pass blocking the keystroke after it. `CATALOG_SEARCH_DEBOUNCE_MS` is 160 — above a fast typist's 120-200 ms inter-key gap, below the ~200 ms at which a delayed UI reads as lag. **The FIELD is never debounced** (the prefill-race rule — debouncing the input itself makes it snap back under the user's fingers); only the query the filter reads. **An EMPTY query applies at once**, so clearing never leaves a stale list, and the `catalogSeed` deep-link applies at once too, since that page's contract is that a search hit opens it PRE-FILTERED.

**(3) `appCtx` — MEASURED, AND THE REPORTED FIX WOULD NOT HAVE HELPED.** The finding was that `appCtx` is a fresh object literal every render, so one save re-runs every mounted view's memos (150-172 ms). Driving the real `App` over a 300-tobacco / 200-pipe / 3000-session cellar, a render that changed **no data** (flipping the Settings tab) cost **63.1 / 53.4 ms — and no view memo re-ran.** `useMemo` compares deps BY VALUE, so a new context identity re-renders consumers without invalidating their memos; and on a SAVE the data genuinely changes, so a memo over `appCtx` would not have stopped that cascade either. The whole cost was two plain **render-body expressions** in `HomeViewV2` (`ghostExclude` / `accordPrefer`) calling `computePipeGhostingRisk` and `pipeAccordsWithFamily` once per pipe — 200 + 200 calls, ~1.3 M session rows — which sit BELOW the view's early return, where a `useMemo` would break hook order, and which are paid only while the Home is on screen (a correction to the "from every screen" framing). The lever is the one `ghosting.ts` already documents: both helpers now take the optional precomputed `PipeCategoryIndex`, built once by the Home. After: **36.5 / 27.0 ms** on a non-data render, **69.1 ms** on a save (was 94.2). **`appCtx` is deliberately left UN-MEMOISED** — a hand-written dep list over **394 keys** (MEASURED — this said « ~200 », roughly half the real figure, and the rule about quoting a number you produced applies to this file's own) is the stale-closure class several builds were spent removing, and it would not have removed this cost. **The lesson is the shape: a finding can name a real symptom, a real cause and the wrong remedy, and only the measurement separates them.**

**FOUR consumers now take that index** (`pipeSessionsSinceMaint`, `computePipeUsageProfile`, `computePipeGhostingRisk`, `pipeAccordsWithFamily`) — if a fifth appears, hoist one index for all of them rather than adding a fifth optional parameter.

## Two writes in one handler, and four other ways a callback wrote too late

**THE MECHANISM, not the instance.** Both stores build their payload from the same render's `data`, and `save` is a `useCallback([])` doing `setData(nd)` — React does not re-render between two calls in one synchronous handler. So a handler that chains TWO store mutations has its FIRST write overwritten, in React state **and on disk**, since `appStorage.set` writes synchronously. Reproduced on `SessionFormView.submit`, the only such handler in the app: the session landed, the lot was debited, and the `tastingNotes` write vanished. Fixed at the mechanism because a per-call-site fix leaves every future two-mutation handler broken: `App.tsx` keeps a **`latestDataRef`**, mirrored at all four `setData` sites (including inside `save`), exposed as a stable `latestData()`; the two stores shadow `data` with it at the top of each mutation. It is only ever AHEAD of `data`, never behind, and it is OPTIONAL so an unwired store degrades silently to the old behaviour — **which is exactly why the wiring is asserted at source level.**

**The legacy inline-base64 photo migration was rebased onto it**, like the trash sweep and the orphan GC: it captured `data`, awaited the IndexedDB writes, then saved from the CAPTURED snapshot, reverting anything done in that window. **A naive rebase is NOT enough, and this is the part worth knowing:** the old swap matched its task by OBJECT IDENTITY (`x.item === item`), and a concurrent save rebuilds every row — so against the fresh cellar nothing would match and the swap would become a silent no-op, with photos written to IndexedDB, never referenced, and the base64 left inline for ever. The pure `applyMigratedPhotoKeys` matches on `(collection, id, dataURL)`; the dataURL is part of the key so a photo the user replaced mid-write is left alone, and it returns the SAME object when nothing applies so the migration cannot dirty the cellar on every launch.

**Three more, each a dead end rather than a race.** **(a) The export could run twice**: the JSZip loader appended a second `<script>` on a second tap, and `_runZip` / `doExport` had no busy latch — one shared in-flight promise and one latch each. **(b) The factory reset raced its own cloud backup**: `save(Object.assign({}, INIT))` arms the 1.2 s debounced auto-save, and the `await imgCache.clear()` after it can outlast that on a photo-heavy cellar, so `gdriveSaveQuiet` uploaded the EMPTY cellar and stamped `cave-autosave-ts`. Fixed by ORDER — auto-save is disarmed before the cellar is blanked, so the debounce has nothing to fire against. **(c) "Restaurer" then "Annuler" muted a genuinely-newer backup for ever**: `_executeCloudNewerRestore` acked the moment the download parsed, writing the persistent dismissed-name marker before the picker had even opened, and `cancelImport` only clears the picker. `stageImport` gained **`onApplied`**, fired after `save(next)` on BOTH modes (a merge puts the file's contents in the cellar too) and try/catch-guarded so a throwing caller cannot take a committed import down with it. **Ack-on-applied was chosen over re-arm-on-cancel deliberately**: re-arming would have to restore previous marker values nothing captured, and would leave a window in which the app has already forgotten a warning it may still need.

**And one clock guard.** `isWithinDays` had no UPPER bound, so `Date.now() - future` is negative and a future timestamp read as "still within" **permanently** — silently disarming the 7-day quota-warning dismissal and the 30-day backup reminder, both of which use it to SUPPRESS. `EB.getDerivedStateFromError` already carried the identical guard, which is what identifies the shape.

## The right data written to the wrong place — five doors, one shape

Something asynchronous finishes and writes into whatever is current NOW rather than into what asked for it. None of the five reports anything: the user sees a fiche they did not edit change under them, or a field they just typed revert.

**(1) A FINISHED AI ANSWER LANDED ON WHATEVER FICHE WAS OPEN.** The provider call has a 60 s abort budget, is not cancelled on navigation, and carried no identity — so: open tobacco A, tap « Rechercher », back out through the unsaved-changes guard, open tobacco B, and A's brand, name, category, blend, force, room note, taste, ageing and description were merged into B. One Save wrote A's data over B's row. `aiLoad` only blocks a SECOND auto-fill; nothing tied the result to the entry that asked. `runAutoFill` now captures `targetId` and all THREE writers (pipe / wish / tobacco) drop the result when the working copy's id has changed — a guard on two of three is a fix for two thirds of the doors, so the test counts them AND counts the writers, or a fourth added later would satisfy the first count while leaving its door open. **RESIDUAL, disclosed: two successive ADD forms both carry `undefined`, so they cannot be told apart this way.** **FERMÉ DEPUIS, et par un compteur plutôt que par une comparaison de contenu.** `src/utils/formSession.ts` tient un compteur de module (`bumpFormSession` / `currentFormSession`) que `nav()` incrémente ; les trois gardes d'écriture d'`useAiAutoFill` comparent désormais `currentFormSession() !== targetSession` en plus de `f.id !== targetId`, donc deux formulaires d'AJOUT successifs se distinguent sans rien savoir de ce qu'ils contiennent — ce qui est exactement ce que le rejet ci-dessus disait impossible AUTREMENT. Verrouillé par `aiFormSessionGuard.test.ts`. Le formulaire d'ENVIE est une superposition que `nav()` ne voit pas, donc ses deux ouvertures appellent `bumpFormSession()` elles-mêmes. Guarding those on the typed brand+name was written and REJECTED — the scan→auto-fill chain builds its query from the SCAN result while the form still holds the previous value, so that rule would have dropped a result the user was waiting for.

**(2) A LATE REVERSE-GEOCODE REVERTED EVERYTHING TYPED DURING A LIVE TASTING.** `tastingSetLocation` did `update(Object.assign({}, tasting, patch))` against the `tasting` captured at TAP time, and the window is a GPS fix plus a Nominatim round-trip — seconds, on a screen whose whole purpose is writing notes. Tap « Capturer la position », then type notes, set a rating, pick aromas: when the lookup landed it wrote the old snapshot back and blanked all of it. **And `writeTasting` persisted the reverted object, so a reload did not recover it.** `onCoords` fires right after the fix and `onPlace` after the lookup, so there were two revert points per capture. Now a functional `setTasting`, with the stage guards moved INSIDE the updater — which is not a detail: they must test the CURRENT state, so a geocode landing after the session was saved cannot resurrect it.

**(3) A FICHE-LOCAL MODAL OUTLIVED ITS FICHE AND RE-BOUND TO THE NEXT ONE.** Both detail views are mounted unconditionally by `CuratorApp` and self-gate with `if (view !== … || !detail) return null`, so they never unmount and their `useState` survives. Open pipe A, tap « Ajouter un entretien », tap a dock tab (`nav()` clears `pipeDet`, the view returns null), open pipe B — the modal is on screen again, over B, holding what was typed for A. `onSave` reads the CURRENT `p.id`, so an ADD landed on B while an EDIT or DELETE targeted an entry id B does not have and silently no-opped, **with the undo toast still shown for a delete that never happened**. `InventoryDetailView` is the same shape at higher stakes — a lot carries a weight, a price and dates and feeds the balance invariants. An effect keyed on the entity id clears them; `nav()` clears `sessionDetail` for exactly this reason and simply cannot see these.

**(4) THE PHOTO CALLBACKS PATCHED A STALE FORM.** `const set = (patch) => setForm(Object.assign({}, form, patch))` captures `form` from the render, and `handlePhotoUpload` is a FileReader → `Image` decode → canvas → IndexedDB chain whose callback fires a fraction of a second after the picker closes — so anything typed in that window was reverted when the photo key was written back. All five form views patch through an updater now. The pipe GALLERY was worse-shaped still: `onChange([...photos, key])` rebuilt from the prop captured at tap time, so a second photo queued before the first landed dropped one; it appends through `onAppend`, which reads the latest.

**(5) THE AI WROTE INCHES INTO A FIELD THE WHOLE APP READS AS MILLIMETRES.** `length` and `weight` legitimately follow the user's unit — their labels interpolate it. The CHAMBER pair does not: `PipeFormView` hardcodes `(mm)`, `PipesDetailView` prints `" mm"`, `chamberVolumeCm3` documents mm and `CHAMBER_DIAMETER_MIN/MAX` are 8-40 mm. They had been given the length field's treatment by mistake, so with the length unit set to inches a 19 mm bowl was stored as `0.75`: the bowl-weight estimate collapsed to 0 and, because `canSave` requires a positive weight, **the session form's Save and the tasting's Ignite stayed permanently greyed with nothing on screen saying why** — while the app's own `chamber_dims_warn` flagged the value it had just written. The test pins that `length`/`weight` still DO convert, or stripping the conversion everywhere would pass and silently store millimetres in a field labelled inches.

**THE PROBE ON (2) STAYED GREEN AND THE TEST WAS AT FAULT, WHICH IS THE METHOD NOTE WORTH KEEPING.** `renderHook` refreshes `result.current` after every `act`, so calling `result.current.tastingSetLocation(…)` at the END invokes the LATEST closure — which already holds the typed values. The reproduction requires grabbing the FUNCTION REFERENCE before the updates, which is what the app really does: it hands `captureTastingLocation` the callback built in the render where the button was tapped. With that, removing the fix reddens exactly the two behavioural cases. *When a probe stays green, find out which layer absorbs it* — here it was the test harness, not the code.

## Four values that made the app never open again

**The class, before the instances.** Each of these arrives through the same ordinary door — a JSON import or a cloud restore of a file you did not write — and each is written to disk by `save()` BEFORE anything renders, so the next launch fails identically. **Neither recovery path helps**: `EB.purgeCachesAndReload` and `public/reset.html` both clear service workers and Cache Storage and deliberately touch neither `localStorage` nor IndexedDB. The only escape was clearing site data or deleting the PWA, which loses the cellar. That asymmetry — a boundary that can recover a bad CHUNK but not a bad VALUE — is why this class is worth its own section.

**(1) A BROWSER THAT REFUSES SITE STORAGE GAVE A BLANK PAGE, AND NOT EVEN THE REPAIR LINK.** Safari (macOS and iOS) with « Block all cookies », Chrome or Firefox with site data blocked for the origin, and some MDM profiles do not hand back an empty store: touching `localStorage` THROWS `SecurityError`. **No file, no import, no hand-editing — a first visit is enough.** `main.jsx` guards every one of its pre-mount reads and its catch says why; `App` did not, so the first render threw. That alone is survivable — the boundary exists for it — except that **`EB.render()` read `cave-lang` from the same store**, in the branch that only runs once something has already crashed. So the boundary threw the identical error while rendering the screen meant to explain it, React unmounted the whole tree, and `#root` was left EMPTY — and the boot shell in `index.html`, **including the « Rien ne se passe ? Réparer l'application → » link**, lives inside `#root` and had already been replaced. A white page with nothing on it. **THE RULE: the error boundary may not depend on anything that can be the thing that failed.** Every storage read on the render path now goes through the crash-safe `lsGet`, swept over `src/` and locked by `storageBlockedBoot.test.tsx`. **The exemptions are stated on the test and are not oversights**: `appStorage.ts` IS the helper; `main.jsx`'s reads are each individually try-wrapped and run before React exists; and the **OAuth / token domain keeps its raw reads** because `no-storage-read-after-remove` matches a LITERAL `getItem` against a literal `removeItem` — route those through a helper and that rule goes blind to the read-before-clear invariant, which is the more valuable guarantee of the two. A case asserts each exempt file still reads storage, so an exemption cannot become dead licence.

**(2) AN OBJECT IN `name` OR `brand` BRICKED EVERY LAUNCH.** `_coerceStringFields` normalised `number` and `boolean` and let everything else fall through, so `"name": {"fr":"N"}` reached React as a child — *Objects are not valid as a React child* — and the Home mounts on every launch. Now TOTAL. **An ARRAY joins and an OBJECT blanks**, which is a decision rather than a detail: `["Halvorsen"]` is a shape a hand-edited or machine-generated file really produces and the word inside it is the user's own data, so blanking would throw away something recoverable; `String({})` is `"[object Object]"`, developer jargon leaking onto a fiche, with nothing to recover. A sweep of every documented string field × ten hostile values through the real migration and the real views found that **only `name` and `brand` on a tobacco or a pipe brick the APP** — the rest degrade one fiche.

**(3) `rating: 1e999` LOCKED THE MAIN THREAD FOR EVER.** `JSON.parse` turns that into `Infinity`, nothing clamped it, and `Stars`' sequenced entry animation registers one timer per star in a `for (i = 1; i <= n; i++)` loop. Opening that fiche hung the tab outright — it had to be force-killed, and the value is persisted, so every later attempt did it again; the user's only escape was deleting the row from the LIST without ever tapping it, which nothing tells them. **The evidence is the reproduction: the test file OOM-killed the vitest worker before the fix.** Fixed in BOTH places on purpose — `migrateData` clamps the stored value, and `Stars` clamps its PROP, because `n` can reach the component by routes the migration never sees (a computed average, a future field) and only five stars are ever drawn, so more than five timers is waste on any input.

**(4) A FORGED `ai-provider` THREW ON EVERY RENDER — and the systemic half matters more than the instance.** `AI_MODEL_OPTIONS[provider]` on a plain object resolves `__proto__` to `Object.prototype`, which is TRUTHY, so the `|| []` fallback never fires and `.filter is not a function`. Lowest reachability of the four (hand-edited storage only — the `_settings` import path validates this key against a closed set). **The ESLint rule written for exactly this class could not see it: `no-dynamic-index-plain-map` bailed on any declaration whose kind was not `const`, and this project declares its lookup tables as `export var` throughout.** Measured: `npx eslint src/hooks/useAiAutoFill.ts` reported ZERO on a live defect. **`var` is now in scope; `let` stays exempt**, which is the distinction the original reasoning was really about.

**WIDENING THAT RULE IMMEDIATELY MET THE OVER-STRICT TRAP, and how it was resolved is the durable part.** It reported SEVEN sites — and **five were already correct**, each carrying an inline `Object.prototype.hasOwnProperty.call(MAP, k)` and a comment explaining it (`FAMILY_AGING_MAX`, `CUT_DENSITY`, `HEADER_ALIASES`, `LANG_ASSETS`, and one more). Flagging them left two bad options: revert the widening and lose the live defect it had just found, or bolt an `eslint-disable` onto five correct sites, **which teaches the next reader to silence the rule**. So the rule learned to recognise the guard: an own-property test on the SAME map, in the SAME statement (`Object.hasOwn` accepted as the modern spelling). **The scoping is deliberate and both halves are pinned as invalid fixtures** — a guard on a DIFFERENT map in the same statement does not launder the read, and a guard several statements away is a control-flow claim this rule cannot verify. **What is NOT accepted is a call to a named predicate that wraps the test** (`isLangLoaded(code)`): following that means resolving an arbitrary function, and the honest answer for such a map is to build it null-prototype — which is what the two genuinely-unguarded sites did (`LANG`, whose own comment used to say *"the rule does not fire here because LANG is a var"*, and `_LANG_COL`).

## The local day — and the five sites that used the UTC one

**`parseLocalDate` has existed for exactly this, its own comment spells the mechanism out, and `daysSince` — the ONE helper feeding every lot age in the app — never went through it.** `new Date("2026-08-24")` is UTC midnight; diffed against a local `Date.now()` it shifts the answer by a day for part of every day. **MEASURED on the real function at every local hour:** Los Angeles wrong from **17:00 to 23:59** (ages one day too HIGH), Auckland wrong **all morning**, and **Paris wrong from midnight to 02:00** (too LOW). So this was never an exotic-timezone corner: the author's own zone is affected every night, and the western window is the EVENING — precisely when somebody smokes a bowl and logs it. `pipeRestDays`, `computePipeRest`, `suggest.ts` and `HomeViewV2` all used the local parser already; this was the survivor. What it drives: `fmtLotAge` on the lot row, the card and the fiche; « ouvert depuis N »; `isRecentPurchase` (so a lot left the « Achats récents » chip a day early); `lotMaturityBucket`'s young→optimal split; `scopedOldestAgeDays`; the CSV export's Age column; « nettoyée il y a N jours » on the pipe fiche.

**`localDayKey(ms)` is now THE local-day formatter and `today()` is `localDayKey(Date.now())`.** Four display/write sites still built a day from `toISOString().slice(0,10)` — the UTC day — surviving the sweep that created `today()`: the **CSV importer's lifecycle back-fill**, which WRITES into the cellar (a western evening import produced a lot « mise en pot » TOMORROW, and nothing warns because `daysSince` clamps a negative age to 0); the **collection report's « Généré le »**, so the document a user files with an insurer was stamped tomorrow and disagreed with the name of the file it arrived in; and two Settings lines (« Catalogue chargé le », the stale-version-check date). `localDayBoundary.test.ts` sweeps `src/` for the CONSTRUCT rather than for any one spelling, **with comments blanked first** — both fixed sites explain themselves by naming what they replaced, and a check that reads its own prose as data is the trap this repo has now been caught by four times.

**THE ACTIVITY STRIP WALKED FIXED 86 400 000 ms STEPS, IN THREE COPIES.** A local day is 23 or 25 hours long twice a year, so the walk SKIPPED a calendar day in spring and emitted one TWICE in autumn — reproduced in Paris and Los Angeles, in both directions. The consequence is not cosmetic: the total above the strip counted a session whose cell was never drawn (measured: `total` 3, cells summing to 2), and tapping the affected cell filtered the journal by the wrong date. `Charts.jsx`'s Stats calendar already walked it with `setDate`; the Home strip did not. **The geometry had THREE hand-rolled copies** — the grid builder, the month ticks, and `HomeViewV2`'s own tap handler — so the fix is one exported `heatmapDayKeys(weeks, nowMs)` every consumer reads, walked with `setDate` at NOON (so the ±1 h a transition moves the wall clock cannot push a date across a midnight of its own).

**A pre-existing test PINNED one of these defects**: `fmtLotAge(364)` asserted `"12age_mo"` — twelve months is a year — two lines above a case named *« caps months at 11 »* that covered the YEAR branch only. The suite asserted a rule and its violation side by side; the reversal is recorded on the assertion.

**THE TEST FORCES A TIMEZONE, and that is the load-bearing part.** The container runs in UTC, where every one of these assertions passes on the OLD code — a test taking the runner's zone would have been vacuous, which is how the defect survived a suite this size. Node re-reads `process.env.TZ` per `Date` call since v16, so setting it in a hook is enough; UTC is kept as the CONTROL, since it is the one zone the old code was already right in and a fix that broke it would show up there.

**DISCLOSED AND NOT FIXED — a clock set forward by more than 30 days empties the trash, unattended.** `sweepExpiredTrash` runs 6 s after launch against `Date.now() - 30 days`, so a device whose clock is a year fast hard-deletes every trashed row and strips `lotId` from their sessions, with no confirmation and no message. Reachability is genuinely low (a dead-battery boot before NTP resync, an emulator, a hand-set date). **The obvious guard is WRONG and that is why this is written down rather than half-fixed**: refusing to sweep when the cut-off is newer than every stamp in the cellar would break the ordinary case of a user who simply has not opened the app in two months — their stamps are all old and the sweep is exactly what should happen. Do not ship that guard without a way to tell the two apart.

**FERMÉ — EN CESSANT DE CHERCHER À LES DISTINGUER.** La note ci-dessus a raison sur le fond et pose une exigence impossible : **aucune donnée locale ne sépare les deux cas**, puisque tous deux mettent `Date.now()` loin devant chaque tampon de la cave. Les seules issues seraient une horloge de confiance (l'en-tête `Date` d'une réponse serveur — donc un réseau que ce chemin, différé de 6 s au lancement, n'a pas) ou une horloge monotone inter-sessions, que le navigateur n'offre pas. Le correctif renonce donc au discriminateur et **borne le rayon** : `trustedSweepNow` (utils.ts) plafonne l'avance de l'horloge du balayage à `TRASH_RETENTION_DAYS` par lancement, contre une marque persistée (`cave-sweep-clock`).

**LA GARANTIE, dans l'ordre où elle compte** : une ligne supprimée DEPUIS le lancement précédent ne peut pas être purgée par un saut d'horloge, parce que la coupure ne peut pas dépasser la marque précédente — c'est ce qui fixe le plafond à 30 jours et pas plus. Qui ouvre l'app au moins une fois par mois n'est jamais bridé, à l'octet près. Une absence réelle de six mois converge en six lancements, et le seul coût entre-temps est une corbeille qui se vide plus tard. Une horloge qui RECULE — la correction NTP après un démarrage aberrant — est absorbée par le `min` : la marque suit vers le bas, donc une mauvaise lecture ne reste pas gravée.

**CE QUE ÇA N'EST PAS, énoncé plutôt que laissé à découvrir** : ce n'est pas une détection d'horloge fausse. Le rayon est borné, pas nul — une ligne qui existait DÉJÀ au lancement précédent peut encore partir en avance. Et la première exécution (marque absente) fait confiance à `Date.now()`, faute de base pour borner : restaurer une sauvegarde sur une machine déjà mal réglée tombe dans ce cas, exactement comme avant. **Le plafond ne descend pas en dessous de 30 jours** : plus bas, quelqu'un qui ouvre l'app toutes les six semaines dériverait indéfiniment et la corbeille afficherait des expirations qu'elle ne tient pas.

**Un cas pré-existant a rougi et il avait raison** : il épinglait `Date.now() - TRASH_RETENTION_DAYS` sur la ligne de coupure. Ce qu'il protège est le SIGNE (un `+` purge toute la corbeille 30 jours trop tôt), pas la provenance de l'horloge ; repointé sur `- TRASH_RETENTION_DAYS`, avec le renversement consigné sur l'assertion. Sondé trois fois — revenir à `Date.now()` nu, ne plus réécrire la marque, retirer le `min` — chacune rougit sa cible et rien d'autre.

## Mesurer plutôt que redouter : dix mutations, dix tuées

**La note voisine dit « 15 des 21 hooks et l'essentiel des ~29 vues NON MESURÉS », et c'était vrai au sens littéral — personne n'avait mesuré. Mesuré maintenant, le risque qu'elle laissait craindre ne s'est pas matérialisé.** Dix règles choisies par CONSÉQUENCE (une rupture qui perd des données ou ment en silence), chacune injectée puis confrontée à la suite ENTIÈRE, la seule question étant « est-ce que quelque chose, n'importe où, l'attrape » : `wishToInv` perdant `catalogueLock` (le verrou par fiche que la passe catalogue en masse doit respecter) · la garde de quota ne regardant plus que le quota d'origine, c'est-à-dire le mauvais budget · un accessoire supprimé DUR, sans corbeille ni annulation · `deleteSession` sans sa garde d'idempotence (grammes crédités deux fois) · `useProgressiveList` remettant son compteur à zéro sur changement d'identité (bouton qui ne révèle plus rien) · `useDbSync` admettant `tastingNotes` dans le diff, le seul champ qui est la prose de l'utilisateur · `isDirty` toujours faux (on quitte un formulaire modifié sans un mot) · `expiresAt` ignoré (annonce périmée pour toujours) · la fiche tabac cessant de suivre la portée du filtre · `canIgnite` n'exigeant plus un lot résoluble — le défaut exact qui a détruit une dégustation entière.

**DIX SUR DIX TUÉES.** Recensement joint : 31 vues sur 33 ont leur propre fichier, et les deux autres sont couvertes par `untestedModules.test.tsx`.

**CE QUE ÇA NE DIT PAS, et c'est la moitié qui compte** : dix règles ne sont pas une couverture. Elles ont été choisies parce qu'elles sont les plus coûteuses à rompre, donc aussi les plus susceptibles d'avoir été gardées. Ce qui reste non mesuré est la QUEUE — les chemins secondaires de ces mêmes hooks, et les règles que je n'ai pas choisies. **La leçon est sur la note plutôt que sur le code : « non mesuré » et « non gardé » ne sont pas la même chose, et confondre les deux fabrique une dette qui n'existe pas.** Une note qui déclare une zone non mesurée doit être refermée par une MESURE, pas par une intuition — dans un sens comme dans l'autre.

## Lot identity across devices — the two silent duplications

**Two ways a LOT was duplicated silently, and the missing net that made both invisible.** Both found by the pre-public-release integrity drill, both reproduced before anything was changed. They share a shape: the same physical tin ends up counted twice, the per-device numeric `id` differs so no id rule fires, and the error is an UNDERFLOW while `lot-balance-overflow` is deliberately one-sided — across ~1500 drilled end-states that rule fired **zero** times.

**HIGH-1 — merging while a lot sits in this device's trash appended the imported twin.** The lot dedup seen-sets (`useImportConfirm`) were built from LIVE lots only, so an imported lot whose twin was in the trash matched nothing. Measured at **16 % of 400 randomised two-device merges**, ~123 g of ghost stock each. The sequence is ordinary: delete a lot on one phone, then merge the other's backup — or tap the cloud-newer banner's « Restaurer ». **Exactly this fix was made one level up, for entities, and lots never got it**; the fix mirrors it — SKIP rather than resurrect (the trashed lot IS this tin, so a copy duplicates it, while un-deleting overrules a deliberate deletion), and COUNT it (`lotsTrashedSkipped` + `merge_recap_trashed_lots`, six languages), because silence reads as « ma sauvegarde n'a pas rendu ma boîte ». A uid-less legacy lot still matches on `lotMergeKey` only — disclosed, not guessed at.

**HIGH-2 — « Tout restaurer » resurrected the source half of a duplicate-merge move.** `mergeDuplicates` — the app's own healing tool for the doubling — moves lots onto the kept row and soft-deletes the originals, with the trash documented as the undo. Measured: 110 g / 2 lots → merge → 110 g / 2 lots → Tout restaurer → **160 g / 3 lots**, invariants silent, one button, no confirm. The same doubling was closed through the per-row door and this one left open. **The signal needs no new field**: the move carries the uid onto the kept lot, so after a move the same uid is LIVE on one lot and trashed on another, and that state has no other legitimate cause. `restoreAllFromTrash` skips those; **`restoreFromTrash` and `restoreSelectionFromTrash` are untouched on purpose** — there the user names one lot, and that choice is theirs.

**The net: `lot-uid-unique`.** The lot `uid` has been the cross-device identity and was **the one identity with no uniqueness rule at all**. Scoped GLOBALLY (a merge moves tins between rows, so per-row scope would miss it) and skipping trashed lots — which is what makes it agree with HIGH-2's rule rather than fight it. It fires on the end state both defects produced.

**Three methodological notes, and the second is the one that generalises.** (a) The new rule shipped without its `Array.isArray` guard and the PRE-EXISTING "survives a garbage payload" case caught it on the first run — the guard is not hypothetical, `data` comes straight from disk or an imported file. (b) **My HIGH-2 test passed while the fix was deleted.** It re-implemented the hook's rule inline instead of driving the hook, and its source-level companion matched `liveLotUids[l.uid]` — which is also how the map is POPULATED. Both were rewritten to drive the real `useTrashOps`; only then does the probe redden. Same lesson as before: *when a probe stays green, find out which layer absorbs it.* (c) A third case expected 160 g after a per-row restore and was WRONG — the restored lot's PARENT row is itself trashed by the merge, so it is not yet in the cellar. The assertion now says that, and says it was corrected.

**Probed in three directions, each reddening alone.** Still open from the same drill and NOT fixed here: the other device's consumption never reaches your stock and the merge reports nothing at all (78 % of merges detach at least one session — the detachment is a deliberate choice, the SILENCE is not); a primitive element in `data.pipes` makes `migrateData` throw on the unguarded auto-apply path; and CSV re-import compounds on a cellar that already holds two same-name rows (28/300, announced each time but the recap does not convey that repeating multiplies). **DEUX DES TROIS SONT FERMÉS ; LE TROISIÈME N'EST PAS VÉRIFIÉ, ce qui n'est pas la même chose et se dit.** (1) Le SILENCE sur les séances détachées a disparu : `MergeSummary` porte `sessionsDetached`, compté au moment du détachement, et le récapitulatif le rend par `merge_recap_detached` — « N séance(s) importée(s) ne sont rattachées à aucun lot : leur grammage n'est pas déduit de votre stock. » Le détachement reste le choix délibéré qu'il était ; ce qui manquait était de le DIRE. (2) L'élément primitif dans `data.pipes` ne fait plus planter `migrateData` : `_keepObj` + `_pruneRows` filtrent les cinq collections avant tout accès, donc le chemin d'auto-application non gardé ne peut plus rencontrer un `5` là où il attend une fiche. (3) **MESURÉ, ET LE RÉSIDU EST RÉEL — mais un mot de la note était faux.** Le pipeline complet a été rejoué (`csvReimportCompounding.test.ts`, rien de simulé au milieu : `buildCsvLines()` → `parseTobaccoCsv()` → `useImportConfirm` en `autoApply:"merge"`), sur une cave de 300 fiches dont 28 engagées dans des paires de même `brand|name` à uid distincts. **Trois imports successifs : 300 → 314 → 328 → 342, soit +14 à chaque passe, indéfiniment.** Le contrôle établit la causalité : la même cave SANS doublon est idempotente (300 → 300 → 300), donc c'est bien le doublon préexistant qui ouvre la porte et non le ré-import en soi. **LE MÉCANISME, nommable avant d'être mesuré** : le CSV ne transporte aucun `uid` (le fichier le dit lui-même — « ni lot ni uid »), donc chaque fiche entrante est anonyme ; `resolveMergeMatch` retombe sur `brand|name` ; et `mergeAmbiguousName` REFUSE l'appariement dès que la cave porte plus d'une fiche de ce nom, parce qu'une ligne anonyme ne peut être attribuée ni à l'une ni à l'autre. **Le refus est correct** — choisir au hasard serait pire, et c'est le même raisonnement que `mergeRefusedByUid`. Ce qui reste ouvert n'est donc pas la fusion mais son ANNONCE. **DEUX CORRECTIONS AUX CHIFFRES DE LA NOTE.** (a) « 28/300 » compte les LIGNES dédoublées ; la croissance est de **14** par passe, parce que `parseTobaccoCsv` regroupe par `brand|name` — 28 lignes sont 14 paires, donc 14 noms ambigus, et le fichier de 300 fiches rend 286 entrées. La croissance suit les NOMS, pas les fiches. (b) **« répéter MULTIPLIE » est faux** : c'est une accumulation LINÉAIRE à taux constant, pas géométrique — dix imports donnent 440 fiches, pas une explosion. Le défaut est réel et non borné ; il n'est pas ce que le mot laissait craindre, et une note qui exagère se fait ignorer aussi sûrement qu'une note fausse. **CE QUI RESTE À DÉCIDER — et c'est une décision de produit, pas une mesure** : `merge_recap_identity` dit « N fiche(s) portent le même nom qu'une fiche existante sans pouvoir y être rattachées : ajoutée(s) séparément, à fusionner à la main si besoin. » C'est exact à chaque passe prise isolément et muet sur le cumul. Aucune correction n'est faite ici : la mesure était demandée, la reformulation en six langues appartient à celui qui connaît maintenant le chiffre.

**THIRD way, and it is the same tin under a DIFFERENT ROW. The lot uid sets were per-tobacco while every other consumer of a lot uid is global.** The trashed-twin fix built its sets from the MATCHED tobacco's own lots, which is the scope `lotMergeKey` needs and not the scope a `uid` has: `lot-uid-unique` says so in its own comment — *"scoped GLOBALLY, not per tobacco, because the tins it protects can end up under different rows (a merge moves them)"* — and `restoreAllFromTrash` walks every tobacco for exactly that reason. **What moves a lot between rows is `mergeDuplicates`, the app's own healing tool for the doubling**, which carries the `uid` onto the kept row and soft-deletes the source copy. So: A and B both hold the pre-uid doubling (T1/T2, same `brand|name`, distinct entity uids, the tin's lot under T2); A merges T2 into T1, backs up; B merges that backup — staged T1 matches B's T1 by uid, B's T1 has no lots, the per-row seen-set is EMPTY and the tin is **APPENDED**, while staged T2's lots are all `deletedAt` and skipped and `deletedAt` is LWW-protected, so B's T2 keeps its live copy. **200 g where the user has 100.** `lot-balance-overflow` cannot see it (deliberately one-sided; this is an underflow) and the recap reported it as an ordinary `merge_recap_lots`; `lot-uid-unique` DOES fire, which is the only reason it was not silent — a post-hoc diagnostic, not a guard. **`lotMergeKey` deliberately STAYS per-tobacco**: acquisition identity (box + price + dates) means something only within one blend, and two different blends bought the same day at the same price are not the same tin — widening it would DROP a real lot, so that half is asserted separately. The global sets are kept LIVE as lots are appended, and an added-wholesale row registers its lots too. Reproduced end to end before the fix; probed by neutering the global registration (8 cases redden, including the trashed-twin ones — the global set subsumes the per-row one).


## A forged imageUrl, and why the guard sits at the choke point

**A forged backup BRICKED the app permanently, and the fix had to be at the choke point rather than the source.** Found by the pre-public-release security review, reproduced by me before being written down. A `.json` backup carrying `imageUrl: "__proto__"` survives `migrateData` (its guard-rail only blanked `^https?://` at the time — see the allowlist that replaced it), and the photo lookup all **18 render sites** share — `(imgLocal && imgLocal[x.imageUrl]) || x.imageUrl` over a PLAIN object — returns **`Object.prototype`**, a truthy non-string. `safeBgUrl` then called `url.indexOf("blob:")` on it and threw.

**What made it the worst finding rather than a crash**: `save(next)` runs BEFORE the render, so the poisoned row is already in `localStorage`; the inventory list throws unconditionally; and **neither recovery path clears localStorage** — `EB.purgeCachesAndReload` and `public/reset.html` both stop at service workers and Cache Storage (verified in both files). One imported file therefore showed the error boundary on every launch, for ever, with the user's own cellar stranded, escapable only by clearing site data or deleting the PWA. Not reachable via CSV (`parseTobaccoCsv` forces `imageUrl: ""`); JSON import and cloud restore only. **This is exactly the class the public release opens** — until now the only person who could hand this app a backup was its owner.

**The same field, the other direction, and the sink `safeBgUrl` does not cover.** The guard-rail left in `migrateData` anchored on `^https?://`, which is the LOSING side of this question: a **protocol-relative** `//evil.com/beacon.png` and a `data:image/svg+xml` both walked straight past it. All 38 background sites route through `safeBgUrl`, which correctly returns `""` for both — but the form's photo PREVIEW is **the one bare `<img src>` in the app**, fed by `imgLocal?.[form.imageUrl] || form.imageUrl`, so a lookup miss puts the raw string in `src`. Opening the edit form of an imported item therefore fired a request to the author's host: IP, User-Agent, and confirmation that the file was imported. Not code execution — an `<img>` cannot run SVG script — but a **tracking beacon inside a shared backup**, and it contradicts `public/privacy.html` verbatim (« Photos (locales uniquement) … ne transitent par aucun service tiers »). Reachable via JSON import (BOTH replace and merge — `migrateData` runs on the merge result too) and via cloud restore; NOT via CSV, which forces `imageUrl: ""`. **The fix is an ALLOWLIST, `isLocalPhotoRef`, because enumerating what is BAD is exactly what had already missed twice from one anchor**: an `imageUrl` has had precisely two legitimate shapes made the app local-photos-only — a `local-photo-*` key, or a `data:image/(jpeg|jpg|png|webp|gif);` URI, which is the **quota fallback** and must keep working (a naive `isSafeExternalUrl` guard here would have broken it, which is why the sink was left unguarded in the first place). Applied at BOTH ends sharing that one predicate — `migrateData` (renamed `_clearForeignImageRefs`, since a name that says "external URL" would now be lying) so nothing poisoned is ever stored, and `safeImgSrc` at the sink so a value arriving any other way still cannot beacon. An unresolved `local-photo-*` key is deliberately still passed to `src`: as a relative URL it 404s on our own origin, no external request, which is the pre-existing behaviour for an evicted photo. Probed three ways (restore the anchor, restore the bare `<img>`, widen the allowlist to any `data:`) — 6, 1 and 4 cases redden respectively. **A case had asserted that `migrateData` does NOT blank `"__proto__"`, with the stated reason that scrubbing it would make the downstream guards pass vacuously — that is exactly what happened, and the reversal is recorded on the assertion**: those guards are defence in depth for a caller handing over a plain map (every fixture, three form views), so they are still exercised directly, without going through the source.

**Same build — the cloud-newer banner said « synchroniser » and did a REPLACE.** `restoreCloudNewerBackup` is the app's only `autoApply: "replace"` call site: it skips the Replace/Merge picker, shows no diff and offers no undo, while all six languages' `cloud_newer_banner` said *restore it to SYNC this device* — and sync is the MERGE word. It matters most for the exact user it appears to, because `findNewerCloudBackup` compares the cloud file against **this device's last CLOUD save, not its last local edit**, so the banner surfaces precisely when there is unsynced local work to lose. The text now names the replacement in each language's own word (remplace / replaces / sustituye / ersetzt / sostituisce / substitui), and a test asserts none of them says "sync" and that there is still exactly ONE `autoApply: "replace"` call site — the wording is only honest while the action stays the one it describes. **SETTLED BY THE USER: it goes through the Replace/Merge PICKER now**, so the app has NO auto-applied replace left in production. The decision it reverses is recorded in `useImportConfirm` («the picker would just add an extra confirm step with no useful choice — there's no other reasonable interpretation»); there IS another one, MERGE, and this is exactly the banner where it matters, since `findNewerCloudBackup` compares against this device's last CLOUD save rather than its last local edit. **`setImportModal(true)` is REQUIRED alongside the staging, not decoration**: the picker is rendered by `SettingsModal`, which `CuratorApp` only mounts while `importModal` is set, so staging without opening it would leave the import pending with NOTHING on screen — a worse defect than the one being fixed. Two tests recorded the reversal on their own assertions (`useGdriveSync.test.ts`'s « the third arg requests an auto-replace, so the user never sees the picker », and the « exactly ONE call site » case, which now asserts ZERO across all three hooks with comments stripped first). The `autoApply` OPTION stays — `"merge"` has a live caller (the CSV import) sharing the same code path. Probed: restoring the auto-replace reddens 2 cases. **The size question the previous sentence left open was settled, and it is NOT "raise Restaurer to 44".** The state was: `Restaurer` at `minHeight: 44` on Home and **28 in the global Overlays copy**, with a **28 px dismiss ×** beside it in a `gap: 8` row. So the HARMLESS action was the harder of the two to hit, 8 px from a button that replaces the entire cellar — and a miss lands on the destructive one. **The house 44 exists so a control can be REACHED, not so a destructive control is easier to hit by accident**, which is why the obvious reading of the rule is the wrong fix here. The rule the two copies now share: **the DISMISS is never smaller than « Restaurer », and they are never adjacent.** The Overlays × goes 28 → 44 and a 14 px `aria-hidden` gutter separates the pair; Home was already 44 on both, so only the separation was missing there. **« Restaurer » stays at 28 in Overlays deliberately** — the smaller target of the two, recorded in a comment so a future sweep does not "complete" the house rule on it. Locked by the « cloud-newer banner: the dismiss is the bigger target » block in `Overlays.test.tsx`, which asserts the × is ≥ 44 **and ≥ Restaurer** (the relative half is the one that carries the rule) and that a gutter element sits between them; probed by reverting both, which reddens it.

**Two fixes, and which one carries the guarantee matters.** `safeBgUrl` is now TOTAL (`String()` on entry): it is the single choke point all 18 sites funnel through, so the guarantee holds **even for a caller that hands it a plain map** — which is what every test fixture does, and what three form views and `useImportConfirm` still could. `imgMap` (`Object.create(null)`) is the root-cause fix and it is DEFENCE IN DEPTH, not the crash fix: it stops a prototype member leaving the lookup at all. Note what it does NOT do — the fallback is `|| x.imageUrl`, so the poisoned string still reaches `safeBgUrl` and the card still shows an empty photo box either way. Choosing the choke point over the source is the whole lesson: **the source is nine sites and the choke point is one.**

**The nine sites are why `imgMap` exists rather than nine inline `Object.create(null)` calls**: the initial state, four updaters in `App.tsx`, one in `useImportConfirm`, and one in each of the three form views — and every one of them was `Object.assign({}, prev, …)`, which throws the prototype straight back on. `imgMapIsSafe.test.ts` sweeps them by keying on the SETTER (`setImgLocal`), never on a spelling of the rebuild, with comments blanked first and a non-vacuity case asserting the setter is still found in each file. `tabac-local/no-dynamic-index-plain-map` could not see any of this: the map is React state, not a module-level `const`.

**Probed four ways, each reddening on its own** — dropping the coercion, giving `imgMap` a prototype, reverting one of the nine sites, and the end-to-end case: rendering the REAL `InventoryListView` over the REAL `migrateData` output reproduces `TypeError: url.indexOf is not a function` on the old code and passes on the new. `forgedImageUrl.test.ts` also asserts `migrateData` still does NOT blank the poisoned value, so the guards it locks cannot start passing vacuously.

**One regression of my own, worth recording because the mock was right to break.** `useImportConfirm.test.ts` mocks `../utils/imgCache.ts` wholesale, so the new export was missing and five `_imageData` cases died. The mock now carries the REAL `imgMap`, not a stub — a stub returning `Object.assign({}, …)` would have made the suite green against a photo map that has a prototype, i.e. it would hide the defect the sibling test exists to lock.

**Open at the time of the same review and NOT fixed then** (each needed a decision, unlike this one) — **all three have since been CLOSED**, and the list is kept rather than deleted so the record of what that review found stays intact: an imported `_apiKey` was written on MERGE as well as replace, so accepting a shared file redirected your AI calls to the sender's billing and console (**since fixed**, on the user's decision); `frame-ancestors 'none'` sat in a `<meta>` CSP where the spec requires browsers to ignore it, with GitHub Pages unable to send the header (**since fixed** — the directive stays, labelled inert, and `frameGuard.ts` is the real defence); and « Effacer toutes les données » left `dropbox-rt`, the three AI keys and `gdrive-account-hint` (an e-mail address) in localStorage, harmless with one user and a mislabel for a public one (**since fixed**).


## A lot says where its grams went

**A lot now says where its grams went, and the feature was the small half of the build.** Reported: « quand je vais sur un lot j'aimerais avoir la date de chaque session avec le grammage fumé avec un lien sur la session ». The gap was real and had a shape worth naming — **the lot fiche stated what was LEFT and never what became of the rest, while the journal could be filtered by tobacco but never by LOT**, so the one screen that raises the question was the one screen that could not answer it. The lot detail modal now lists its sessions (date · time · grams, newest first), each row a real button that opens that session's own fiche.

**Three things were EXTRACTED or REUSED rather than written a second time**, which is this file's most repeated lesson. (a) `compareSessionsRecent` (utils.ts) is JournalView's inline `sortBy === "date"` comparator, moved: two of its four rungs are not re-derivable by eye — timed-before-untimed is handled EXPLICITLY because locale collation does not rank punctuation against digits predictably, and the final `id`-DESC tie-break is what keeps the order stable after a restore scrambles `data.sessions`. (b) `sessionsForLot` owns the filter, `String()`-ed on BOTH sides because the lot `id` is numeric and `session.lotId` is a string — a raw `===` matches nothing and would render an empty list on a lot that has been smoked. It refuses an EMPTY lot id rather than gathering every orphan under whichever lot is open. (c) the row's accessible name is `aria_session_card`, the key the journal cards already use, so the two doors to a session announce identically.

**`crossOpenSession` (App.tsx) is the counterpart of `crossOpenDetail` for the one overlay that is not an entity fiche, and it needed NO change to the back engine.** It records the origin with `pushDrillOrigin` and deliberately does NOT set `drillOverlayRef`: that latch means "the open OVERLAY was drill-opened" and `decideBack`'s pop branch lists only detail/pipe/acc/wishlist, so claiming it for a session modal would assert something the branch cannot see. The sequence falls out of the existing rules — first back hits `close-session`, second finds a non-empty stack and pops to the origin. The lot modal's state is deliberately LEFT ARMED on the way out: the fiche's subtree unmounts on nav so nothing is on screen, and the pop re-opens the very lot the user was reading.

**The modal moved to `capHeight`, which the rule requires the moment a modal can exceed the screen** — a 100 g tin can carry forty bowls. `capHeight` + an inner `flex: 1; minHeight: 0; overflowY: auto; overscrollBehavior: contain`, never a `vh` cap.

**A TOTAL was considered and deliberately NOT shown, and rendering it is what settled the question.** The modal already gives initial and current, whose difference IS the grams smoked; a third figure can disagree with them (a hand-edited weight, a disposed lot, rounding). In the render fixture the seven sessions summed to 17.6 g against a 19 g drop — exactly the confusion a total would have manufactured.

**THE OTHER HALF OF THE BUILD: rendering the modal found a PRE-EXISTING clip that three checkers cannot see.** In German the Close/Delete/Edit row needed **349 px in a 340 px box at the DEFAULT text size** (375 at "L") and the third button read « BEARBE », cut at the panel edge — measured identical at HEAD, so it is not from this change, and it bites an ordinary German phone at the default setting rather than at some extreme. They are single words, so a flex item at its default `min-width: auto` could only clip; `flexWrap` costs a line ONLY where the three do not fit (verified in a browser: one line in fr / en / pt, two in de) and clips nothing — the remedy for the same arithmetic on the list controls rows. **The screen IS in the checkers' list (`modal-lot`), which is what makes the gap worth recording: `i18n:layout` fails on a page-level `scrollWidth`, on `nowrap`/ellipsis text, and (more recently) on a draggable horizontal scroller — and this is none of the three.** Content simply painted past a hidden-overflow panel. **Not fixed here** (widening a checker mid-feature is the wrong moment, per the rule about refusal sets) but it is the next thing to close: an element whose `scrollWidth` exceeds its `clientWidth` inside an `overflow: hidden` ancestor is invisible to every rule we have. **FERMÉ DEPUIS : c'est la QUATRIÈME règle d'`i18n:layout` (`cutOff`).** Elle ne mesure pas le `scrollWidth` de l'élément — elle remonte jusqu'au premier ancêtre qui découpe réellement et compare la GÉOMÉTRIE à cette frontière, parce que la feuille porte le texte et l'ancêtre décide si le débordement est atteignable. Une troncature SIGNALÉE (`text-overflow: ellipsis`) est exemptée : elle appartient à la règle `clipped`, et la confondre avec une coupe silencieuse ferait condamner un usage intentionnel. Verrouillée par `hiddenOverflowCutoff.test.ts`, qui épingle deux défauts préexistants que la règle a trouvés en arrivant.

**Probed in seven directions**, each reddening on its own: dropping either load-bearing rung of the comparator, keeping soft-deleted sessions, neutering the row handler, dropping `capHeight`, dropping the overscroll containment, passing every session unfiltered, and removing the wrap. **One probe came back green and the FIXTURE was at fault, not the code** — the timed-before-untimed case used ids whose DESC order happened to agree with the right answer, so the tie-break absorbed the mutation; the ids were inverted so the later rung now contradicts the earlier one. Same shape as the photo cap: *when a probe stays green, find out which layer absorbs it.* **A process note worth keeping: `git checkout <file>` was used to undo a probe on an UNCOMMITTED file and reverted the whole feature with it.** Recovered from a scratch copy. Undo a probe by restoring the copy you took, never with `git checkout`, while the work is unstaged.


## The session mechanic — which lots a session may charge

**The session mechanic audited end to end, and THREE of its findings were one root cause: two predicates disagreed about which lots a session may charge.** `pickJarLot` accepted any jar; `isUsableLot` refuses a jar carrying an EXPLICIT `"0"` (an empty tin). The disagreement is reachable and was **REPRODUCED before anything was changed**: a tobacco with a 0 g jar AND a full cellar lot is offered in both session pickers (the cellar lot makes it usable), and the auto-select then landed on the EMPTY JAR — which is absent from the picker's option list. **A `<select>` whose value matches no option displays its FIRST one**, so the screen showed the cellar lot while the state held the empty jar, and the session was capped to 0 g against it. From there the two visible symptoms follow: a live tasting could be ignited pointing at a lot that resolves to nothing, and 95 minutes later the auto-end **DESTROYED it** — `_persistSession` refuses a positive weight with no resolvable lot, and the auto path clears the tasting state whether the save succeeded or not (a stuck zombie tasting being the worse outcome), then announced « clôturée automatiquement après N min ». A whole dégustation lost, under a success sentence. **Four fixes, and they are deliberately layered rather than one clever guard**: `pickJarLot` now takes `isUsableLot` (still jar-only — that is its contract, and `_persistSession` refuses a cellar lot outright); the duplicated auto-select block became `pickSessionLot` in `lotUtils` — **best usable jar, else the oldest usable CELLAR lot — so the chosen lot is guaranteed BY CONSTRUCTION to be one the picker will list**, which is what makes the display/state divergence impossible rather than merely unlikely; `canIgnite` requires a lot that RESOLVES among the usable ones (not merely a non-empty `lotId`, since a setup persisted across relaunches can point at a lot finished since); and the auto-end says `tasting_err_autoend_failed` when the save did not happen. **The gate and the message are both needed** — one stops it happening, the other stops the app lying about it if it ever does. Three smaller ones in the same pass: the auto-end **no longer navigates** (`addSessionFromTasting(form, {navigate:false})`) because it fires from a timer while the user is elsewhere, possibly mid-form, and a direct `nav()` bypasses `formGuardRef` — only `goBack` consults it; the tasting's failed cellar-open now REPORTS itself (`tasting_open_err`) instead of silently dropping the pending state, which its `SessionFormView` twin has always done; and `fmtLotWeight` gives an absent weight ONE rendering (`—`) where the app had three (`0g` on the lot row — asserting a figure the user never gave — `—` in the detail modal, and a hole in the picker label), keeping `0g` for a lot genuinely weighed at zero so the distinction stays visible on screen. `tobaccoHasUsableLot` is also live in production at last: exported, unit-tested and DEAD since it was written, the `tobaccoHasTag` shape that knip cannot see because a test counts as a consumer. **Probed five ways; the third probe came back GREEN and was VACUOUS** — it prefixed `false &&` onto the branch while the test asserts the SOURCE contains `auto && !ok && setSaveError`, so the substring survived. Redone by deleting the branch outright, it reddens. Same lesson as the two probes above: **a probe that does not change what the assertion actually reads is not a probe.**

**FOUR readers of the untracked-weight notion were fixed and there were SIX, and the fifth one turned the fix into a silent data loss.** An audit of the whole session mechanic (live + manual), each finding re-verified before acting. **(a) THE FIFTH READER: the three weight CAPS in `useSessionStore`.** `_persistSession` and both branches of `updateSession` clamp the recorded weight to what the lot holds (`Math.min(w, safeW(lot.weightG))`) — a guard against recording more than exists — and `safeW("")` is 0, so every session logged against a lot that fix had just made offerable **recorded 0 g**. Worse than the bug it followed: the tobacco now appears in the picker, the session saves, the journal shows it, and the grams are gone with no message. **Un-correctable by editing**, since the edit path clamps the same way. All three now exempt an untracked lot — the cap protects a KNOWN balance, and there is nothing to protect when there is no balance. **(b) THE SIXTH READER: a local COPY of the predicate in `InventoryDetailView`** (`(parseFloat(l.weightG)||0) > 0`), which the fix left behind — so the fiche went on printing « renseigne le poids d'un lot pour pouvoir utiliser ce tabac dans une séance » about a tobacco both pickers were already offering. The copied-predicate failure this repo keeps paying for (the tag predicate ×4, `FAMILY_AGING_MAX`, `CATS`). **The hint is KEPT, not deleted, and REWORDED in all six languages**: the state that still warrants it is an explicit ZERO — a weight typed by hand, or a finished lot reopened at 0 — which the pickers do refuse, and « renseigne le poids » was the wrong sentence for it because the weight IS filled in. Both halves of that reversal are pinned (zero raises it, unweighed does not), on the assertion itself so it is not "fixed" back. **(c)** The cellar auto-select fallback was duplicated verbatim in `SessionFormView` and `TastingView` and read the raw weight too; both go through `isUsableLot` now. **(d) A CROSS-CHANNEL defect, found while reading the same area and unrelated to weights: the storage-full warning could be disarmed for SEVEN DAYS by a gesture that has nothing to do with it.** `saveWarn` is a SHARED channel — the tasting auto-end notice was put on it, and the `save()` QuotaExceeded migration has been on it far longer — while the banner's × wrote `cave-quota-warn-dismissed` **unconditionally**. So closing « Dégustation clôturée automatiquement après 95 min » silenced « le stockage est à 80 %, sauvegardez avant que les écritures échouent » for a week, silently. The dismissal moved into `useStorageQuotaWarning` gated on the same `raisedRef` the clearing branch has: **the hook is the only place that knows whether the banner on screen is ITS banner**, so the write belongs there and nowhere else; the banner now REPORTS the dismissal (`ctx.dismissQuotaWarn`) and a second close cannot re-arm it. Probed in all four directions, each mutation verified applied and reddening exactly its own cases.

## Le document qui évinçait ses propres lecteurs

**`CLAUDE.md` faisait 1 082 836 caractères — environ 270 000 jetons — et il est injecté EN ENTIER dans chaque session ET dans chaque sous-agent.** Un agent démarrait donc avec ce socle avant d'avoir lu une ligne de code, et une tâche de SONDAGE est la plus coûteuse qui soit : elle enchaîne lecture de grosses vues, mutations et exécutions répétées de la suite. **MESURÉ : trois agents de sondage sur quatre sont morts de saturation de contexte** avant leur première sonde ou peu après, là où trois agents de nettoyage lancés plus tôt avaient tenu 125, 79 et 66 appels d'outils. Le seul survivant était celui dont la cible tenait en une vue.

**Trois consignes d'hygiène explicites n'ont rien changé** — pas de `Read` au-delà de 300 lignes, `grep` plutôt que lecture du document, redirection des sorties de test. Ce n'était pas un problème de discipline : le socle est consommé avant que la première consigne puisse s'appliquer. **Le fichier qui porte la connaissance du dépôt était devenu assez gros pour évincer les agents qui devraient s'en servir**, ce qui est la forme la plus coûteuse d'une documentation excellente.

**LE DÉCOUPAGE EST VERBATIM, ET C'EST LA DÉCISION QUI COMPTE.** La tentation évidente est de RÉSUMER : ce document est fait de narration, chaque paragraphe pourrait tenir en trois lignes. Elle a été écartée, parce que ce qui coûte cher ici n'est pas le *quoi* mais le *pourquoi* — quelle correction a été rejetée, quelle sonde est restée verte et quelle couche l'absorbait, quel chiffre a été mesuré et lequel a été hérité. Un résumé garde les conclusions et jette exactement la partie qui empêche de refaire l'erreur. Les sections ont donc été déplacées telles quelles, et la vérification est un comptage de LIGNES : **zéro ligne perdue**, +32 lignes ajoutées (les six en-têtes et l'index).

Le noyau retenu est **94 Ko (~23 k jetons, −91 %)** : la vue d'ensemble, la pile technique, le contexte React et son invariant dur, la sécurité, le service worker, les sous-sections normatives du flux de travail (portes, versionnage, règles du changelog, conventions git) et les 26 conventions. Six fichiers `docs/*.md` portent le reste. **La règle de partage n'est pas la taille mais la QUESTION à laquelle la section répond** : le noyau répond à « que dois-je faire à chaque tâche », `docs/` répond à « comment ça marche » et « pourquoi c'est comme ça ».

**LA MOITIÉ RISQUÉE N'EST PAS LE DÉPLACEMENT, C'EST QUE LES PORTES CESSENT DE VOIR LEUR SUJET.** Quatre portes de `doc:check` grepent ce document — tout module de `src/utils/` y est nommé, la même pour `src/hooks/`, toute clé de stockage figure dans sa table, et l'arbre du dépôt existe sur le disque. Privée de son texte, chacune ne rapporte RIEN, c'est-à-dire se lit comme un dépôt propre : la plus vieille panne de ce dépôt (`prune` rouge pendant neuf versions sous une prose qui l'annonçait vert). `docChecks.DOC_FILES` liste les sept fichiers et `doc-check.cjs` en construit la CONCATÉNATION, donc chaque porte garde exactement le même sujet.

**LA LISTE VIT DANS LE MODULE PUR ET NON DANS LA PORTE**, parce qu'elle a trois lecteurs — la porte, `docChecks.test.ts` (qui analyse l'arbre) et `appSettings.test.ts` (qui vérifie la table des clés) — et qu'une liste écrite trois fois est la dérive que ce dépôt paie en boucle. `docFiles.test.ts` INVERSE LA CHARGE plutôt que de redire la liste : il parcourt les `docs/*.md` que **git suit** et exige que chacun soit couvert, donc un septième fichier ne peut pas partir non gardé. Sondé deux fois — retirer un fichier de `DOC_FILES` le nomme, redéclarer une copie locale dans `doc-check.cjs` rougit la garde de câblage.

**DEUX GARDES ONT MORDU PENDANT L'OPÉRATION, ET C'EST LE MEILLEUR ARGUMENT POUR LES AVOIR ÉCRITES.** Sept des huit contrats d'étiquette numériques pointant `CLAUDE.md` sont passés au rouge à l'instant où leur paragraphe a bougé — chacun nommant son nombre, son ancien fichier et donc, implicitement, le travail restant ; le huitième (`diagnostic-max-recent`) est resté vert parce que sa phrase est bien restée dans le noyau, ce qui prouve que la porte lisait vraiment. Et `browserPathFilter.test.ts` a signalé les six nouveaux fichiers comme un répertoire de premier niveau non classé : de la documentation ne peut pas déplacer un pixel, donc ils rejoignent `INERT` avec leur raison — une exemption qui NOMME sa raison, jamais un dénombrement.

**RÉSIDU ÉNONCÉ : le découpage réduit le socle, il ne le supprime pas.** Un sous-agent démarre encore avec ~23 k jetons, et `docs/architecture.md` (390 Ko) reste trop gros pour être lu d'un bloc — il se grep, il ne se lit pas. Si un agent meurt encore, la mesure à refaire est la même : compter le socle avant d'accuser la consigne.

## La porte cassée par la prose qui l'expliquait

**Vingt minutes après avoir écrit « une porte qui cesse de voir son sujet ne rapporte RIEN », j'ai cassé la porte 9 en écrivant exactement cela.** L'index de `CLAUDE.md` explique ce que fait la porte et le fait en la citant : « gate 9 still slices the tree out of `## Repository Structure` wherever that section now lives ». Or `extractTreePaths` cherchait le titre par SOUS-CHAÎNE. La première occurrence dans la concaténation est devenue cette phrase, la clôture suivante venue était une autre clôture, et l'analyse rendait **zéro chemin et aucune erreur** — donc `doc:check` affichait « OK » en n'examinant plus l'arbre du tout.

**Ce n'est pas une coquille, c'est la cinquième instance d'une classe que ce dépôt a déjà nommée** : une garde qui lit sa propre prose comme une donnée. Les quatre précédentes sont `topBannerInset` (une bannière fantôme lue dans un commentaire), la porte 15 (une liste d'exemple prise pour un axe de langue), la porte 9 de l'extraction i18n (`t("…")` cité dans un commentaire) et `layoutGutters`. Chaque fois le remède a été de blanchir les commentaires ; ici il n'y a pas de commentaires, il y a du markdown — et la propriété qui distingue les deux est plus simple encore : **un titre markdown est une LIGNE qui commence par `## `, une mention dans une phrase ne l'est pas.** La recherche est ancrée en début de ligne (`^…$` multiligne), le titre étant échappé puisqu'il vient d'un paramètre.

**LA MOITIÉ QUI A RENDU LA PANNE INVISIBLE EST L'AUTRE.** Une clôture analysée qui rend zéro chemin remontait `errors: []`, c'est-à-dire un succès. Les deux pannes possibles — mauvaise clôture, ou arbre vidé — se lisaient donc l'une comme l'autre comme un dépôt propre, ce qui est précisément le mode d'échec que la porte existe pour empêcher. Zéro chemin est désormais une ERREUR.

**COMMENT ELLE A ÉTÉ TROUVÉE, parce que ce n'est pas par la porte :** `docFiles.test.ts`, écrit dans le même commit pour garder le découpage, exige `paths.length > 50` sur la concaténation — il a rougi pendant une exécution de la suite lancée pour une sonde sans rapport. **La porte, elle, restait verte.** C'est l'argument pour avoir doublé la garantie côté vitest : `doc:check` ne pouvait pas s'auto-diagnostiquer, puisque le défaut consistait justement à ne rien avoir à dire. Sondé dans les deux sens : rétablir la recherche par sous-chaîne rougit 3 cas (dont les deux qui portent sur le VRAI document), retirer la non-vacuité en rougit 1.

## Douze règles que rien ne gardait, et elles sont toutes du CÂBLAGE

Le découpage de `CLAUDE.md` avait un but : rendre les sous-agents de sondage viables. Trois ont été relancés aussitôt après, sur des cibles étroites (≤ 4 règles chacun, sonde par CONSÉQUENCE contre la suite entière) — `SettingsModal`, les modales Courses / Comparer / Recherche, et les quatre hooks de démarrage. **Les trois ont tenu, là où trois sur quatre étaient morts avant. Douze règles sondées, DOUZE non gardées.**

**LE MOTIF EST UN SEUL, ET IL EST DÉJÀ NOMMÉ DANS CE DÉPÔT : la logique est éprouvée là où elle est DÉFINIE et non gardée là où elle est APPELÉE.** `cloudEncryptionWiring` couvre le chiffrement, `useImportConfirm` la fusion, `imgGcGating` le ramassage de photos, `progressiveList` la fenêtre progressive — tous bons. Ce que rien ne testait, c'est **quel bouton appelle quoi, avec quel argument, sous quelle condition**. Un moteur juste derrière un fil croisé se comporte exactement comme un moteur cassé. C'est la septième et la huitième instance de cette forme (après `chooseAutoSaveTarget`, `reDeductRestoredSessions`, `findParityGaps`, `useUnsavedFormGuard`).

**La plus chère : la carte « Fusionner ».** Recâblée sur `applyImport("replace")`, la suite entière reste verte — et l'ancienne suite `SettingsModal.test.tsx`, 92 cas, reste verte elle aussi (vérifié séparément). La carte dont le texte promet « Rien n'est effacé » effacerait la cave entière, sans confirmation et sans corbeille : la corbeille garde des suppressions unitaires, pas un remplacement de base. Les deux cartes sont à trente lignes l'une de l'autre et se ressemblent trait pour trait ; un seul mot les sépare.

Les onze autres, par coût décroissant : **« Importer la sélection » sans son `selectedSet`** (on coche 2 tabacs sur 400, on en reçoit 400, et une fusion ne s'annule pas) ; **`useOrphanPhotoGC` qui ne visite pas les accessoires** (chaque photo d'accessoire effacée d'IndexedDB au lancement suivant — `imgGcGating` couvre tabacs/pipes/envies mais toutes ses données portent `accessories: []`) ; **l'interrupteur de chiffrement dans ses deux sens** (l'éteindre sans confirmation envoie les sauvegardes en clair ; l'allumer sur une invite ANNULÉE laisse « chiffrement actif, phrase absente », état dont `cloudEncryptionWiring` mesure lui-même l'effet : l'auto-save ne monte plus rien, sans message) ; **`useProgressiveList` qui ne débranche pas l'observateur précédent** (les `IntersectionObserver` s'empilent, la fenêtre double à chaque coup d'œil, et le gel de 13 s revient par la porte de derrière — le faux observateur du test existant a un `disconnect` no-op, donc par CONSTRUCTION il ne peut pas voir l'empilement) ; **`useLotIntegrityProbe` qui recompte** (le compteur de diagnostic grimpe seul et annonce une corruption qui s'aggrave alors que rien n'a bougé) ; **la sourdine de 7 jours de `useStorageQuotaWarning` qui n'expire pas** (les cas existants ne posent que des sourdines datées de MAINTENANT : ils vérifient qu'elle fonctionne, jamais qu'elle prend fin) ; **la relecture de `cave-shopping-checked` au montage** (l'écriture était gardée, la relecture non : en magasin, un rechargement fait disparaître les coches) ; **`copyList` qui copierait les lignes cochées** (on rachète ce qu'on vient d'acheter) ; **le `setCatalogSeed(q)` d'un hit catalogue** (atterrissage en haut d'un catalogue non filtré, la correspondance qu'on venait de voir introuvable) ; **la croix de `CompareModal`** (elle nomme sa colonne, donc elle promet — et le test existant ne cliquait qu'avec UNE colonne, où toute croix retire la bonne : la règle n'était vérifiable qu'à partir de deux).

**DEUX MUTATIONS ONT ÉTÉ ÉCARTÉES COMME ÉQUIVALENTES, ce qui est la moitié honnête du protocole** : le plafond `COMPARE_MAX` est déjà tronqué en amont par `buildComparison`, et le filtre `deletedAt` de `CompareModal` reçoit `liveData`, déjà filtré — les retirer ne change rien en production. Une survivante n'est une trouvaille que si la mutation change vraiment le comportement.

**CE QUE J'AI VÉRIFIÉ MOI-MÊME plutôt que sur rapport**, parce qu'un agent qui se trompe se trompe avec aplomb : les trois affirmations principales, chacune en opposant l'ANCIENNE suite à la NOUVELLE sous la même mutation — 92 cas verts contre 1 rouge pour la fusion, 17 contre 2 pour les coches de courses, 28 contre 1 pour les photos d'accessoires. Et une correction dans deux des trois fichiers rendus : les fixtures nommaient des mélanges RÉELS ; remplacés par le vocabulaire inventé du dépôt.

**RÉSIDU ÉNONCÉ, dans les mots des agents** : `GDriveConfirmPanel` (choix de la sauvegarde à restaurer) et `SyncDiagView.onDeleteEntry` (suppression d'un fichier cloud ligne à ligne) sont deux surfaces destructrices du même fichier, non sondées faute de budget. Et une remarque de conception qui vaut mieux qu'un test : `applyImport(mode)` sans second argument signifie « tout prendre » — une signature dont l'ABSENCE d'argument est le comportement le plus large est un défaut par défaut.

## Le sélecteur de sauvegarde, vérifiable seulement à partir de DEUX lignes

La restauration cloud est la surface la plus destructrice de l'application : son bouton « Importer » remplace la cave entière, et sa propre ligne le dit — « Remplace tout. » Trois de ses règles ont été sondées ; **une était gardée, deux ne l'étaient pas, et les deux survivantes tiennent au même angle mort.**

**Gardée** : la corbeille par ligne de `SyncDiagView`. Rediriger `onDeleteEntry(r.id)` vers `rows[0].id` rougit immédiatement — le cas existant supprime délibérément la SECONDE ligne « so the id asserted below is not the first one by accident ». C'est exactement la précaution qui manquait ailleurs.

**Non gardées** : (1) la radio du sélecteur — forcer `sel: 0` à chaque clic laisse la suite entière verte, et l'utilisateur qui choisit la sauvegarde d'avant-hier parce que celle d'hier était incomplète restaure celle d'hier ; (2) les trois lectures `options[gdriveConfirm.sel]` — les remplacer par `options[0]` laisse aussi tout vert, et les deux avertissements (« cette sauvegarde semble vide », « elle contient moins de données que votre version locale ») décrivent alors une AUTRE ligne que celle qui sera restaurée. Un garde-fou qui rassure sur un fichier qu'on ne va pas prendre, ou qui alarme sur un fichier qu'on n'a pas choisi, est pire qu'absent : on apprend à ne plus le lire.

**L'ANGLE MORT EST LE MÊME QUE CELUI DE LA CROIX DE `CompareModal`, ET IL MÉRITE D'ÊTRE NOMMÉ COMME UNE CLASSE : un sélecteur monté avec UNE SEULE option, ou avec `sel: 0`, ne distingue pas « la bonne » de « la première ».** Les cas existants du sélecteur montent tous `sel: 0` — ils vérifient sincèrement que l'avertissement de rétrécissement s'affiche, et ne peuvent structurellement pas voir qu'il s'affiche pour la mauvaise ligne. La règle n'existe qu'à partir de deux, avec un choix qui n'est pas le premier. **Quand un test porte sur « celui qui est désigné », le montage doit contenir au moins deux candidats et désigner le second.**

## Une confirmation devant le seul geste sans filet

Des trois cartes du sélecteur d'import, « Remplacer » est **la seule qui efface** : « Fusionner » n'enlève rien, « Annuler » renonce. Et elle est **la seule sans filet** — la corbeille garde des suppressions unitaires, pas un remplacement de la base entière, et l'annuler de 8 s ne couvre pas ce chemin. Elle a donc reçu un `window.confirm`, comme « Vider la corbeille » et « Retirer le catalogue ».

**Ce n'est PAS un retour aux dialogues retirés en 23**, et la distinction est la seule chose qui compte ici : ceux-là gardaient des SOFT-deletes, qui ont un annuler de 8 s et une corbeille de 30 jours derrière eux — confirmer un geste réversible apprend à taper « oui » sans lire, ce qui abîme la confirmation du geste qui, lui, ne l'est pas. La règle du dépôt n'a jamais été « pas de confirmation », elle a toujours été « une confirmation exactement là où le filet manque ».

**`window.confirm` et non une modale maison**, pour la raison déjà écrite à propos de la réinitialisation : c'est de la chrome de navigateur, donc non recouvrable par un cadre hostile. **Fail-closed** : un `confirm` refusé — ou supprimé par le navigateur, qui rend alors `false` — ne remplace rien ; ouvrir par défaut serait la pire des deux erreurs sur une action irréversible. Sondé dans les deux sens, la moitié positive incluse : sans elle, une confirmation qui n'accepte JAMAIS passerait le cas négatif et la restauration propre deviendrait impossible.

## Ce que le découpage a cassé sans que rien ne le dise

Quatre audits parallèles ont relu le noyau, les six annexes, le guide utilisateur et les trois autres documents publiés, chacun **en mode rapport seulement** — les corrections ci-dessous ont été vérifiées une par une avant d'être appliquées, et l'une des trouvailles était fausse (voir la fin).

**LA CLASSE DOMINANTE EST CELLE QUE LE DÉCOUPAGE A CRÉÉE, ET ELLE ÉTAIT PRÉVISIBLE : vingt-quatre commentaires du code disaient « voir la note dans CLAUDE.md » pour un texte parti dans une annexe.** Un renvoi qui nomme un fichier est vérifiable ; un renvoi qui nomme un fichier **et se trompe** envoie le lecteur chercher ce qui n'y est plus, ce qui est strictement pire que pas de renvoi du tout — il coûte une recherche ET détruit la confiance dans les autres. Deux d'entre eux (`gdriveApi.ts` « Drive refactor », `useDbSync.ts` « deferred-unmount ») ne désignaient déjà **plus rien nulle part**, donc ils étaient morts avant le découpage. Cinq *labels* de `label-contracts.json` avaient la même maladie. **La leçon de méthode : un déplacement verbatim ne casse pas le texte, il casse tout ce qui le DÉSIGNE — et rien n'automatise cette vérification.**

**Six renvois `§N` étaient faux pour une raison indépendante et plus ancienne : l'insertion de la convention `#17b` (devise) a décalé la numérotation** et personne n'a relu les commentaires qui citaient un numéro. `§19` désignait « Tasting mode » à la place des invariants de sécurité, `§12` « Image storage » à la place du cycle des lots. **Une liste numérotée à la main est une base de données sans clé étrangère** ; le seul remède réaliste est de citer le TITRE et non le numéro, ce que font désormais les six.

**DEUX CONTRADICTIONS INTERNES, chacune entre deux passages du même fichier.** (1) Le bloc de code d'ouverture montrait `export type AppCtxType = Record<string, any>` et une graine vide, pendant que le paragraphe vingt lignes plus bas explique que « `AppCtxType` est entièrement typé — gardez-le ainsi ». Le bloc datait d'avant le typage et personne ne l'a relu en écrivant l'anti-régression qui le dément. **Une esquisse a le droit d'abréger, pas d'annoncer le contraire de la règle qu'elle illustre.** (2) La liste blanche OAuth était énoncée à **cinq** valeurs à un endroit et à **huit** — les vraies — à l'invariant #20. Le lecteur qui tombe sur la première croit que trois actions légitimes sont des attaques.

**LE GUIDE UTILISATEUR ENSEIGNAIT DEUX COMPORTEMENTS QUE L'APP N'A PAS, et le plus cher n'était pas une erreur mais un SILENCE.** La section « Mises à jour » ne décrivait que le chemin manuel ; or l'application détecte une nouvelle version, affiche un bandeau, **décompte dix secondes et se recharge toute seule** — et un second chemin s'installe sans aucun bandeau. Un utilisateur dont l'app redémarre sous les doigts sans que le guide l'ait annoncé conclut à un plantage. Même forme pour « Remplacer » : l'import recharge l'application à la fin dès que le fichier porte des préférences (elles ne sont lues qu'au démarrage), et rien ne le disait. **Une documentation qui omet un redémarrage ne cache pas un détail, elle transforme un fonctionnement normal en panne apparente.** S'y ajoutent trois affirmations fausses : « Remplacer » présenté comme le mode PAR DÉFAUT du sélecteur (c'est la dernière carte, en rouge, et rien n'est présélectionné), trois formes coniques citées sur les six que le code applique, et une note tronquée dupliquée dans cinq langues sur six.

**LA PORTE DU CHANGELOG A ÉTÉ CONTOURNÉE PAR UN ADJECTIF.** Elle comparait le sous-titre à une liste de chaînes exactes, donc « Correction **importante** » passait — mesuré : l'entrée du build 25 s'auto-classe comme correctif dans les six langues et la porte rendait zéro erreur. C'est la classe des exemptions qui énoncent un DÉNOMBREMENT au lieu d'une PROPRIÉTÉ : la propriété réelle est « le titre CONTIENT un mot qui classe l'entrée ». Découpage par MOTS et non `indexOf`, sinon « prefix » contient « fix » et la porte ferait supprimer des entrées correctes. **Le portugais manquait aussi de la liste** : la porte lisait cinq sections sur six et se taisait sur la dernière.

**UNE TROUVAILLE SUR QUATRE-VINGTS ÉTAIT FAUSSE, et c'est la raison de tout vérifier.** Un agent a signalé que `docs/integrations.md` décrit `decideBack` en s'appuyant sur `locHasFiche`, une fonction qui n'existe pas. Elle n'existe effectivement pas — **parce que le paragraphe suivant du même fichier raconte sa suppression.** Le texte était juste ; l'agent avait lu le nom sans lire la phrase qui l'entoure. Une deuxième correction, la mienne, a mal tourné pour une raison voisine : mon script de déduplication a reconnu la note à supprimer par un mot voisin plutôt que par son contenu, a effacé une note anglaise légitime et manqué l'italienne. **Reconnaître une chose par ce qui l'entoure plutôt que par ce qu'elle est — c'est la même erreur des deux côtés.**

## Escamoter la chrome, et les trois objections dont deux étaient fausses

Demandé : le geste de Facebook — les barres qui s'effacent au défilement et reviennent quand on s'arrête. Ma première réponse a été de le limiter au dock, avec trois objections contre l'escamotage de la barre du HAUT. **L'utilisateur a poussé, et deux des trois ne tenaient pas.** Elles méritent d'être écrites parce qu'elles se ressemblent : les trois étaient plausibles, aucune n'avait été VÉRIFIÉE.

- **« On perd le bouton retour » — FAUSSE sur le périmètre retenu.** Sur les quatre racines de liste, le `leading` de la `TopBar` est une icône DÉCORATIVE (feuille, pipe, livre, flamme). Seule `CatalogView` porte un vrai `IconBtn icon="back"` — elle est donc exclue, et l'objection devient un critère de périmètre au lieu d'un refus.
- **« On perd la recherche et les filtres » — FAIBLE.** La règle « tout mouvement vers le haut révèle » les rend avant qu'on y arrive. C'était un argument POUR la clause de révélation, pas contre la fonctionnalité.
- **« La mesure `data-topbar` casse » — FAUSSE.** Le seul site de mesure lit `getBoundingClientRect().height` — une HAUTEUR. Une translation ne la change pas ; un `scaleY` l'aurait changée. La distinction tenait en une ligne de lecture.

**LA GARDE DU DOCK A MORDU, ET ELLE AVAIT RAISON DEUX FOIS.** `iosPwaDockGuard` protège la promotion de couche `translateZ(0)` de la bande extérieure, gagnée à la main contre une dérive de rendu iOS. Composer la translation d'escamotage avec elle l'a fait rougir — mais parce que l'assertion épinglait une ORTHOGRAPHE (`transform: "translateZ(0)"`) et non la propriété. Une réécriture équivalente la cassait, ce qui fait supprimer du code correct. Elle exige désormais que **chaque état** de la ligne porte la promotion : strictement plus fort, puisqu'un ternaire dont une seule branche l'oubliait passait avant de ne plus rien correspondre. Sondé — la demi-rupture rougit maintenant, ce que l'ancien motif ne pouvait pas voir.

**ESLINT A REFUSÉ LA PREMIÈRE VERSION DU HOOK, ET SA RÈGLE VALAIT MIEUX QUE MON CODE.** `react-hooks/set-state-in-effect` a signalé deux `setState` synchrones dans des effets : la remise à zéro au changement de vue, et la lecture initiale de la media query. Les deux avaient un motif prévu que je n'avais pas employé — l'ajustement d'état EN PHASE DE RENDU pour la première (React le traite sans rendu supplémentaire), `useSyncExternalStore` pour la seconde, une media query étant littéralement un magasin extérieur. **Et j'ai failli ne pas les voir : mon premier passage écrivait `npx eslint … | tail -3; echo $?`, qui a affiché `0` alors que la porte sortait à 1** — exactement le piège que `CLAUDE.md` documente, commis dans la session qui le documente.

**« LE BUG DU DOCK FLOTTANT EST-IL RÉINTRODUIT ? » — LA QUESTION A PAYÉ, MÊME AVEC UNE RÉPONSE NÉGATIVE.** Non, et pour une raison structurelle : le garde-fou décisif est le PORTAIL (`createPortal(<BottomDock …>, document.body)`), vérifié intact, donc ni la coquille ni la `TopBar` ne sont ancêtres du dock et aucune propriété d'ancêtre ne peut l'attraper. Mieux : la `TopBar` portait DÉJÀ `backdrop-filter`, qui figure dans la liste même des propriétés créant un bloc conteneur — elle en était donc déjà un, et lui ajouter un `transform` ne change rien en nature. Aucun `position: fixed` ne vit dans son sous-arbre.

**Mais la question a fait tomber deux `will-change: transform` que je n'aurais pas dû écrire.** Sur le dock il était strictement redondant — le `translateZ(0)` juste au-dessus promeut déjà cette bande, délibérément et avec son argumentaire. Sur la `TopBar`, c'était une promotion de couche PERMANENTE posée sur un élément flouté et collant, c'est-à-dire exactement le type de chrome sur lequel ce dépôt a documenté une dérive de rendu iOS ; et `will-change` figure lui aussi parmi les propriétés créant un bloc conteneur, donc en poser un de plus dans la colonne demandait un gain — qu'une translation de 220 ms sur un élément simple n'offre pas. Les deux retirés. **La leçon n'est pas « le doute était fondé » mais l'inverse : une vérification qui infirme la crainte peut quand même trouver quelque chose, à condition de regarder le code plutôt que de rassurer.**

**RÉSIDU ÉNONCÉ, et c'est une inégalité assumée.** `usePrefersReducedMotion` ne couvre que cette animation. Les sept autres transitions de l'app ignorent la préférence. La distinction est défendable — celle-ci est la seule bande pleine largeur, les autres sont des micro-mouvements sur un bouton pressé — mais elle reste une inégalité, et la généraliser est un chantier à part qui n'a pas été fait ici.

## Un critère énoncé, un autre appliqué — et un test creux qui protégeait la mauvaise chaîne

Retour d'usage : l'escamotage ne se déclenchait pas sur l'accueil, les statistiques, le catalogue et les accessoires. Quatre pages, quatre situations DIFFÉRENTES, et il a fallu les séparer pour voir laquelle était un défaut.

- **Accessoires — aucun défaut.** L'escamotage y est actif ; la page était simplement trop courte, `CHROME_MIN_OVERFLOW_RATIO` exigeant 0,75 hauteur d'écran à défiler. L'utilisateur avait posé l'hypothèse lui-même et elle était juste.
- **Catalogue — exclusion voulue**, sa barre porte l'unique sortie (`IconBtn icon="back"` → `nav("inv")`).
- **Statistiques et accueil — ma faute, et d'une forme précise : J'AI ÉNONCÉ UN CRITÈRE ET APPLIQUÉ UN AUTRE.** Le critère annoncé était « la barre du haut ne porte aucune sortie » ; ce que j'ai codé était « les quatre racines de LISTE ». Or le `leading` de `StatsView` est un `Orn` décoratif, exactement comme les quatre autres. Le mot « liste » s'était glissé dans la règle sans jamais être justifié, et personne — moi le premier — n'a rapproché les deux phrases. **C'est la variante la plus discrète de l'erreur : le raisonnement est écrit juste au-dessus du code qui le contredit.** Le périmètre est désormais les SIX racines que le dock atteint.

**LE HOME EST UN SECOND SITE D'APPEL, ET IL ÉTAIT INVISIBLE.** `HomeViewV2` n'utilise pas le primitif `TopBar` : elle bâtit son en-tête à la main, avec la même recette collante et floutée. Câbler `primitives.tsx` ne l'atteignait donc pas. Elle hérite bien de `--chrome-shift` (son en-tête descend de `<main>`), mais il fallait le lui demander. Une garde de câblage propre la couvre maintenant, sur le principe déjà payé six fois ici : une fonctionnalité qui marche à un site d'appel ne dit rien du second.

**ET LA VÉRIFICATION A TROUVÉ UN TEST CREUX CHEZ MOI, sur l'exclusion la plus importante du lot.** Le cas « le catalogue n'escamote pas » assertait `canAutoHideChrome("catalogue")` — or la vue s'appelle **`catalog`**. Il mesurait donc une chaîne qui n'existe NULLE PART dans l'application : il passait pour une mauvaise raison, et serait resté vert même si le vrai `catalog` avait été ajouté au périmètre. La garde censée protéger la seule sortie du catalogue ne protégeait rien. Réparé, et surtout **inversé** : les identifiants du périmètre sont désormais confrontés à ceux que les vues DÉCLARENT (`view !== "x"`), donc un identifiant fantôme est refusé. Sondé — ajouter `"catalogue"` au périmètre rougit maintenant, là où rien ne bougeait avant.

**La leçon des deux défauts est la même et vaut d'être nommée : une chaîne inventée passe partout.** `"catalogue"` dans un test, « liste » dans un commentaire — ni l'un ni l'autre ne correspondait à quoi que ce soit d'exécutable, et les deux ont survécu à une relecture parce qu'ils SEMBLAIENT désigner la bonne chose.

## Une fiche n'est pas une vue — et trois chaînes inventées l'ont caché

Question posée : « sur les fiches tu as aussi appliqué ? ». Réponse : **oui, à tort, et c'était livré.**

`InventoryDetailView` se garde par `view !== "inv"`, la fiche pipe par `"pipes"`, l'accessoire par `"acc"`. **Une fiche ne change donc PAS de vue** : c'est un SOUS-ÉTAT (`detail` / `pipeDet` / `accDet`) rendu sous la vue de sa liste. Une fiche ouverte héritait par conséquent du périmètre de sa liste et escamotait la barre qui porte son `IconBtn icon="back"` — **la seule sortie**. C'est mot pour mot le défaut pour lequel `catalog` est exclu, reproduit sur trois pages de plus.

**TROISIÈME CHAÎNE FANTÔME DU MÊME FICHIER, et celle-ci avait une conséquence.** Le cas d'exclusion assertait `canAutoHideChrome("detail")`, `"pipeDet"`, `"accDet"` : trois valeurs qui ne sont pas des identifiants de vue. Il mesurait trois situations impossibles pendant que la vraie — une fiche ouverte sur sa liste — passait. La garde anti-fantôme écrite au commit précédent ne couvrait que le PÉRIMÈTRE, pas la liste d'exclusion ; elle les couvre toutes les deux maintenant, et le relevé d'identifiants lit aussi les `nav("x")` pour que les pages de documentation ne comptent pas comme fantômes.

**LE GUIDE AVAIT RAISON ET LE CODE AVAIT TORT**, ce qui est l'inversion habituelle. La note d'aide promettait depuis le premier jour que « les fiches, les formulaires, la dégustation et le catalogue ne sont pas concernés ». La documentation décrivait l'intention ; rien ne vérifiait que le code la tenait. Aucune entrée de changelog pour ce correctif : la description publiée reste exacte, elle n'a jamais annoncé les fiches.

**Ce que je retiens de la série — trois fantômes, un critère mal appliqué, un site d'appel oublié — tient en une phrase : une chaîne inventée passe partout, et un test qui la contient ne mesure rien tout en ayant l'air de mesurer l'essentiel.** Les trois fois, l'assertion nommait exactement la bonne CHOSE et exactement la mauvaise VALEUR.

## Le critère était déjà dans le code — j'en tenais une copie

Question de l'utilisateur : « il inclut bien les fiches aussi ? Le catalogue ? Toutes les pages où se trouvent les menus finalement ». La dernière proposition est le vrai apport : **« là où les menus sont » est un critère, et il existait déjà — c'est `shouldShowDock`.**

Je tenais à côté un `AUTO_HIDE_VIEWS` écrit à la main. **En trois commits, cette copie a produit TROIS chaînes fantômes** : `"catalogue"` (la vue s'appelle `catalog`), puis `"detail"`, `"pipeDet"`, `"accDet"` (des sous-états, pas des vues). La dernière n'a pas seulement rendu un test creux — elle a **livré** le défaut que le périmètre existait pour empêcher, en escamotant le bouton retour des fiches. Une liste d'identifiants recopiée s'accorde toujours avec elle-même et jamais avec l'application.

`canAutoHideChrome` **dérive** désormais : `return shouldShowDock(view, gate)`. Il n'y a plus d'identifiant à écrire, donc plus d'identifiant à inventer — **la classe de bug disparaît avec la liste, pas avec le dernier bug**. Le test ne compare plus à une énumération : il vérifie que les deux décisions s'accordent sur TOUS les identifiants que les vues déclarent, et que la coquille remet la MÊME porte aux deux fonctions — car une règle dérivée reste défaite par deux états différents au site d'appel. Sondé : réintroduire une liste manuelle rougit en nommant `catalog`.

**ET MON OBJECTION D'ORIGINE ÉTAIT PLUS FAIBLE QUE JE NE L'AI DITE, ce qui est la partie inconfortable.** J'excluais les fiches et le catalogue parce que leur barre porte le bouton retour — présenté comme « la seule sortie ». Vérification faite après la question : `useBackNavigation` câble le retour SYSTÈME (bouton matériel, geste de bord) et `decideBack` sait fermer une fiche ; et le bouton n'est jamais absent plus d'une demi-seconde puisque tout mouvement vers le haut ET l'arrêt le ramènent. J'avais énoncé une contrainte forte sans mesurer sa force réelle, et elle a servi à justifier un périmètre plus étroit que nécessaire.

**Trois questions de l'utilisateur, trois défauts trouvés** — le dock qui ne flottait pas mais deux `will-change` de trop, l'accueil et les statistiques laissés dehors par un critère mal appliqué, les fiches escamotées à tort. Aucune n'était une suggestion : les trois demandaient une vérification, et c'est la vérification qui a payé à chaque fois.

**LA BARRE DU HAUT ÉTAIT TROUBLE, ET SON DÉGRADÉ LA RENDAIT AUSSI INVISIBLE AU VÉRIFICATEUR DE CONTRASTE — deux défauts pour un seul décor.** Rapporté par l'utilisateur d'un seul mot — « elle est trouble » — sur une capture d'iPad. Le fond de `TopBar` était `linear-gradient(180deg, C.bg, alpha(C.bg,"cc"))` plus un `backdrop-filter: blur(8px)`, sous un commentaire affirmant que ce mélange « empêche le contenu défilé de transparaître ». **`cc` vaut 80 %**, donc un cinquième de ce qui passe dessous arrivait à l'écran. **MESURÉ plutôt que discuté** : la même barre rendue deux fois — à vide, puis avec la liste poussée dessous — et comparée pixel à pixel. **2,05 % de la barre changeait, écart maximal 56.** À 92 % d'opacité : 0,23 %. Opaque : **0 %**. 2 % semble peu et ne l'est pas, parce que ce qui passe n'est pas uniforme — les listes sont pleines de photos de boîtes claires, et c'est exactement ce que montrait la capture. **LA MOITIÉ QUE PERSONNE NE POUVAIT VOIR.** `theme-contrast.cjs` porte `if (gradient || !bg) { out.skipped++; continue; }` — on ne calcule pas un rapport de contraste contre un fond qui varie. Le titre de cette barre n'était donc **mesuré sur aucun des 35 écrans qui la portent**, depuis que la barre a ce dégradé. Fond rendu plat : **2558 → 2593 éléments mesurés, +1 sur exactement 35 écrans**, le 36e (`help`) n'ayant pas de `TopBar`. Le décor translucide ne troublait pas seulement l'utilisateur : il soustrayait son propre titre au contrôle. **ET LE COMPTEUR D'OMISSIONS EXISTAIT SANS ÊTRE RAPPORTÉ.** `out.skipped` était calculé et imprimé nulle part ; s'il l'avait été, 35 éléments sautés par palette auraient sauté aux yeux. Il est désormais affiché à côté du total — **59 restent sautés** ailleurs (94 avant), sur d'autres dégradés, et ce reste est ÉNONCÉ et non cherché dans ce build. **Un total de mesures sans son total d'omissions se lit comme une couverture ; il n'en est pas une.** **LE FLOU PART AVEC LE DÉGRADÉ**, et pas seulement parce qu'il devient inutile derrière un fond opaque : `backdrop-filter` crée un BLOC CONTENEUR, la propriété même qui a déjà fait tomber le dock fixe dans le flux sur la PWA iOS. Trois barres collantes partageaient la recette — `TopBar`, l'en-tête de `FormFields`, celui de `HomeViewV2` — et les trois sont traitées ensemble : une seule corrigée aurait été une incohérence visible d'un écran à l'autre. **LE TEST QUI VERROUILLAIT LE DÉFAUT** s'appelait « with a frosted background » et exigeait `linear-gradient`, sous un commentaire disant « un fond assez opaque pour que le contenu ne transparaisse pas » — l'assertion tenait exactement ce qui causait le défaut, au nom de l'intention inverse. Renversée sur place, jamais effacée, et elle exige maintenant la PROPRIÉTÉ (pas de dégradé, pas de `backdrop-filter`) plutôt que l'orthographe d'un token.

**LA BARRE DU HAUT ÉTAIT TROUBLE DANS L'APP INSTALLÉE ET NETTE DANS SAFARI — la même page, le même build, le même iPad.** C'est cette paire de captures qui a tranché, et elle a tué DEUX diagnostics que j'avais défendus avant elle. **(1)** Le fond translucide des barres : réel, mesuré (2,05 % des pixels changeaient sous le contenu défilé), corrigé au build 15 — et **sans le moindre effet** sur le symptôme rapporté. **(2)** La promotion de couche due au `transform` de l'escamotage : argumentée, plausible, et fausse — ce `transform` est là dans Safari AUSSI, et Safari est net. **Une cause qui ne distingue pas standalone de navigateur ne peut pas expliquer une différence entre standalone et navigateur** ; il a fallu une capture de l'utilisateur pour que je le voie, pas un raisonnement de plus. **LA CAUSE** : `apple-mobile-web-app-status-bar-style: black-translucent` fait passer le contenu SOUS la barre d'état, et iOS pose alors son propre voile par-dessus pour garder l'heure lisible quel que soit le fond. Ce voile tombe sur la bande où vit la barre du haut. Dans Safari, la chrome du navigateur occupe cette bande — rien n'est superposé. **Aucun CSS de l'app ne pouvait le corriger** : le voile est peint par le système après toute la composition. La ligne datait du 25 août ; ce qui a changé, c'est iOS 27 — l'utilisateur l'avait proposé d'emblée et je n'y avais pas cru assez. **`default` PLUTÔT QUE `black`**, qui aurait aussi réservé la bande : `black` la figerait en noir alors que l'app a un mode clair, et `theme-color` est DÉJÀ tenue en phase avec le mode par App.tsx (`THEME_COLOR_META`), son propre commentaire la nommant « iOS status bar / letterbox ». `default` laisse cette synchronisation, déjà correcte, piloter la bande. `viewport-fit=cover` RESTE : il porte encore les côtés en paysage et l'indicateur d'accueil. **VÉRIFIÉ DEPUIS, ET LE CORRECTIF FONCTIONNE — mais le rôle de la réinstallation N'EST PAS ÉTABLI, et j'avais entre-temps déclaré le diagnostic MORT.** Mesuré sur le titre de la barre du haut, même téléphone, même écran : **avant réinstallation `rgb(157,127,85)` lum 130 ; après réinstallation `rgb(224,195,145)` lum 198 ; Safari `rgb(224,195,145)` lum 198**. Pixel pour pixel identique à Safari — même triplet, même luminance, même position. **LA LEÇON EST UNE ERREUR DE RAISONNEMENT, PAS DE CODE.** L'utilisateur a répondu « toujours trouble » après le build 16 ; j'en ai conclu que le diagnostic était faux et j'ai cherché ailleurs pendant cinq échanges. Or le correctif n'avait peut-être jamais été appliqué. **ATTENTION, LE TEST EST CONFONDU et je l'ai d'abord raconté comme s'il ne l'était pas** : la réinstallation a changé DEUX choses à la fois — l'installation ET la version chargée — et l'utilisateur n'avait jamais confirmé tourner sur le build 16 avant de dire « toujours trouble ». Ce qui est établi est donc « build 16 + réinstallation fonctionne », PAS « la réinstallation est nécessaire ». J'ai affirmé qu'iOS lit `apple-mobile-web-app-status-bar-style` À L'INSTALLATION ; c'est une inférence que je n'ai pas prouvée, et plusieurs sources secondaires soutiennent l'inverse (la balise serait relue au chargement). **À TRANCHER PAR UN TEST NON CONFONDU** : un second appareil, jamais réinstallé, ayant récupéré le build ≥ 16 par la mise à jour ordinaire. Une seule variable change. Tant que ce test n'a pas eu lieu, ne pas répéter que la réinstallation est requise. **Un correctif qui exige une étape d'installation n'est pas réfuté par un rechargement** — j'avais l'information et je ne l'ai pas utilisée. Avant de déclarer une hypothèse morte, vérifier que le test l'a seulement atteinte. **CE QUI A SUIVI N'EST PAS PERDU**, et c'est ce qui rend le diagnostic final solide plutôt que chanceux : la promotion de couche a été écartée par une vraie mesure (en-tête de formulaire SANS `transform` terni exactement autant, 135 contre 201 — rapport 0,67 identique à celui de la barre de liste), et le voile a été CARACTÉRISÉ en faisant défiler l'app sous la bande. Le contrôle interne y est parfait : **une même citation en italique coupée en deux par la frontière**, première ligne à 18-50, deuxième à 111-168, corps du texte à 212-238 — un dégradé sur les ~107 pt du haut, SOUS les glyphes de la barre d'état (qui restent à 255) et PAR-DESSUS tout ce que l'app dessine. Aucun CSS ne l'atteint ; seule l'installation décide. **FAUSSE PISTE À NE PAS REFAIRE** : j'avais d'abord conclu « le fond n'est pas assombri, seuls les glyphes le sont, donc ce n'est pas un voile ». Le fond passait de 19 à 14 — cinq unités au plancher de l'échelle, c'est-à-dire dans le bruit de quantification. Un rapport mesuré sur des valeurs quasi noires ne distingue rien. **ET LA GARDE A FAILLI ÊTRE CREUSE, pour la TROISIÈME fois dans la même journée.** Le cas « garde `viewport-fit=cover` » faisait un `toContain` sur tout le fichier — or le long commentaire que je venais d'écrire au-dessus de la balise contient cette chaîne, si bien que retirer l'attribut de la VRAIE balise laissait le test vert. Sondé, reproduit, resserré sur la balise. Même forme que le pointeur `npm run i18n:layout` satisfait par le sommaire du fichier, et que la première version de la garde des replis : **une garde satisfaite par la prose qui la décrit ne garde rien.**

**« TU N'ARRIVES PAS À COLLER LE MENU TOUT EN HAUT ? IL Y A DE LA MARGE ENCORE » — et la marge en question se décompose en trois parts dont une seule était à nous.** La capture envoyée était tirée vers le bas (rebond élastique) : dans cet état le document entier descend, barre collante comprise, donc l'écart visible entre le haut de l'écran et la barre contient l'amplitude du geste et **ne se mesure pas**. Ce qui se mesure est la géométrie INTERNE de la barre, indépendante de l'appareil. **MESURÉ dans Chromium sur la barre réelle, 395 × 859** : hauteur 66 px = 14 (plancher) + 44 (rangée de cible tactile) + 8 (bas), et le pavé de glyphes du titre à **28 px sous le haut de la barre** — 14 de plancher plus 13,5 parce qu'un libellé de 17 px est centré dans une rangée de 44. **Ces 27,5 px sont TOUT ce que l'application dessine au-dessus du titre** ; le reste de la bande appartient à iOS, et aucune CSS n'y entre.

**LE PLANCHER DE 14 px ÉTAIT JUSTE ET NE L'ÉTAIT PLUS, pour une raison qui vient du build 16.** Il protégeait d'une collision avec l'horloge sous `black-translucent`, où iOS superposait ses glyphes au contenu web. Depuis que le manifeste déclare `default`, iOS place la vue web SOUS la barre d'état, `env(safe-area-inset-top)` vaut 0 en autonome, et il n'y a plus rien à heurter. **Le plancher ne gouverne d'ailleurs QUE ce cas** : dès qu'un inset existe, `safeTop` rend l'inset moins le retrait, donc un retour éventuel à `black-translucent` reprendrait la protection tout seul. C'est pourquoi la baisse porte sur le plancher et non sur `TOP_INSET_TRIM_PX` — **on relâche la garde là où la menace a disparu, pas là où elle dort.** 14 → 6 px, vérifié de la même façon sur la barre reconstruite : hauteur **58 px**, titre à **19,5 px**, soit les 8 px annoncés et une décomposition confirmée plutôt que supposée.

**LES 13,5 AUTRES NE SONT PAS RÉCUPÉRABLES, et l'écrire vaut mieux que de le redécouvrir.** Réduire le `padding` du bouton de titre ne les rend pas : `minHeight: 44` tient la hauteur et le centrage recentre. Seul un alignement en haut les prendrait, au prix d'un titre décollé du milieu de sa propre cible tactile, et 44 px est le minimum d'accessibilité — il ne se négocie pas contre 13 px de marge. **ET LE LITTÉRAL ÉTAIT EN TROIS EXEMPLAIRES** : les trois en-têtes de page portaient `safeTop("14px")` chacun de leur côté, si bien que « remonter le menu » demandait de les retrouver tous les trois — la classe de dérive que ce dépôt a déjà payée six fois, réapparue **dans le correctif même qui l'avait supprimée pour les quatorze autres sites**. Un `HEADER_TOP_FLOOR` partagé la referme, et le recensement qui le garde est DÉRIVÉ : il lit les arguments réellement passés à `safeTop` dans tout `src/` au lieu de les redire, donc un nouveau plancher le fait rougir **en nommant sa valeur**. Sondé deux fois — un en-tête revenu au littéral, un calque passé de 10 à 11 px — rouge dans les deux cas, fichiers restaurés à l'empreinte près.

**J'AI COURU UNE PORTE PLUS ÉTROITE QUE CELLE DE LA CI PENDANT TOUTE UNE SESSION, ET LE DÉPLOIEMENT DU BUILD 19 EST MORT DESSUS.** Le script de mesure `geom19.cjs`, déposé à la racine le temps d'une sonde, y est resté et est parti dans le commit. La CI a rougi deux fois : `eslint .` sur 17 erreurs (`require`, `document`, `localStorage` non définis dans un fichier sans configuration Node) et `browserPathFilter.test.ts` sur un fichier suivi que le filtre de chemins ne couvre pas. Aucune des deux ne s'était manifestée ici, et les deux raisons méritent d'être écrites.

**LA PREMIÈRE EST LA GRAVE : la porte que je lançais n'était pas la porte.** `npm run lint` vaut `eslint .` — `docs/checks.md` le dit à sa ligne 13, « ESLint on the WHOLE repo ». Je tapais `npx eslint src scripts eslint-rules vite.config.js eslint.config.js`, une énumération qui a l'air exhaustive et qui **ne visite jamais la racine**. Elle rendait 0 erreur / 7 avertissements, exactement le chiffre attendu, donc rien dans le résultat ne signalait que la cible différait. Le dépôt avait déjà écrit la règle — « lire le code de sortie de chaque porte, jamais un résumé tuyauté » — et j'ai lu un code de sortie authentique, d'une **autre commande**. **Un code de sortie ne certifie que ce qui a été lancé** ; la cible fait partie de la porte au même titre que le seuil. La leçon n'est pas « relire la liste de chemins », c'est **lancer le script npm**, parce que c'est lui que la CI lance et qu'une énumération recopiée diverge — la classe de dérive que ce document décrit partout ailleurs, arrivée cette fois à la commande de vérification elle-même.

**LA SECONDE EST ATTENDUE et vaut d'être sue pour ne pas la prendre pour un trou.** `browserPathFilter.test.ts` balaie `git ls-files` : un fichier NON SUIVI lui est invisible. Il ne pouvait donc pas rougir avant le `git add`, et il a rougi au premier endroit où le fichier était suivi — la CI. C'est son fonctionnement correct, pas une faiblesse ; ce qui manquait en local, c'était `eslint .`, qui lit le disque et l'aurait vu tout de suite.

**ET LE FICHIER A SURVÉCU À SON PROPRE NETTOYAGE, par un mécanisme à ne pas refaire.** La commande finissait par `pkill -f "vite preview --port 4173" ; rm -f ./geom19.cjs`. **`pkill -f` compare le motif à la ligne de commande complète de chaque processus — y compris celle du shell qui l'exécute**, laquelle contient ce motif puisqu'elle contient la commande entière. Le shell s'est tué lui-même et le `rm` n'a jamais eu lieu ; le code 144 rapporté était ce suicide, que j'ai lu comme un bruit de `pkill`. Déposer un script de sonde à la racine d'un dépôt dont la CI lint la racine est l'erreur d'origine — le répertoire de travail temporaire existe pour ça, et la seule raison de la racine était la résolution de `node_modules`, qui se règle avec `NODE_PATH` et non en polluant le dépôt.

**CE QUI EST FAUX DANS L'HISTOIRE, ÉNONCÉ PLUTÔT QUE CORRIGÉ EN SILENCE** : le message de `dd6c847` affirme « eslint 0 erreurs / 7 avertissements ». C'était vrai de la commande que j'ai lancée et faux du dépôt — `eslint .` y rendait 17 erreurs. Le commit est sur `main` et son message ne se réécrit pas ; il est démenti ici, à l'endroit où on le cherchera.

**LE TEST NON CONFONDU A EU LIEU, ET IL ME DONNE TORT : LA RÉINSTALLATION N'EST PAS REQUISE.** Rapporté par l'utilisateur — « Test sur un autre iPhone. Pas eu besoin de recharger ». Un SECOND appareil, jamais réinstallé, passé au build ≥ 16 par la mise à jour ordinaire, affiche la barre correcte ; et pas seulement sans réinstallation, **sans même un rechargement manuel**. `apple-mobile-web-app-status-bar-style` est donc relue au CHARGEMENT et non figée à l'installation. J'avais affirmé l'inverse par inférence, sans jamais le prouver, alors que plusieurs sources secondaires disaient déjà le contraire — et je l'avais écrit dans DEUX fichiers avant d'avoir la mesure. Les deux sont corrigés.

**CE QUI RESTE VRAI DU PREMIER TEST, ET C'EST LA PARTIE QUI COMPTE** : il était bel et bien confondu — il changeait l'installation ET la version chargée — et on sait maintenant lequel des deux faisait tout. **La VERSION.** « Toujours trouble » après le build 16 décrivait simplement un appareil qui ne l'avait pas encore reçu. Donc le diagnostic `black-translucent` était juste dès le départ, il n'a jamais été réfuté par quoi que ce soit, et les cinq échanges passés à chercher ailleurs l'ont été parce que j'ai lu un « toujours trouble » comme une réfutation sans vérifier que le test avait seulement atteint l'hypothèse. **Avant de déclarer une hypothèse morte, vérifier que le test l'a seulement atteinte** — cette leçon-là tient, et elle se renforce.

**MAIS LA LEÇON QUE J'EN AVAIS TIRÉE ÉTAIT LA MAUVAISE, et c'est le retournement à enregistrer.** J'avais écrit : « un correctif qui exige une étape d'installation n'est pas réfuté par un rechargement ». La phrase est vraie comme principe et **fausse ici** : ce correctif n'exigeait aucune étape d'installation. J'avais construit une explication correcte de la forme du raisonnement (le test n'atteint pas l'hypothèse) sur une prémisse factuelle que je n'avais pas mesurée (« iOS lit la balise à l'installation »). **Une bonne leçon adossée à un fait inventé reste à moitié fausse, et c'est la moitié qu'on cite ensuite.** La version qui survit ne dépend d'aucune prémisse sur iOS : vérifier quelle version tourne réellement AVANT d'interpréter un rapport d'utilisateur.

**CONSÉQUENCE PRATIQUE, puisque c'est ce qu'on viendra chercher** : il n'y a rien à dire à l'utilisateur dans `help.html` sur une réinstallation, et aucune note d'installation à écrire. La mise à jour ordinaire suffit — ce que l'app fait déjà toute seule.

## Les 59 omissions : deux défauts, et un raisonnement à moitié juste

**« VÉRIFIER LES 59 ÉLÉMENTS » — et la première chose mesurée est qu'ils n'étaient pas ce que la doc en disait.** Ce fichier portait *« 59 restent sautés ailleurs, sur d'autres dégradés, et ce reste est ÉNONCÉ et non cherché dans ce build »*. Énoncé, oui ; exact, non. La sonde donne **59 par palette, identiques sur les six, et 100 % des dégradés — zéro fond indéterminable**. La ligne de rapport annonçait pourtant *« skipped (gradient or indeterminate backdrop) »* : **la moitié de la phrase décrivait une classe vide**, et personne ne pouvait le savoir puisque le compteur donnait un total sans jamais nommer un seul élément. Un compteur qui ne sait pas dire *lesquels* ne se diagnostique pas — la même leçon que le total par écran, une couche plus bas.

**LE RAISONNEMENT D'ORIGINE ÉTAIT À MOITIÉ JUSTE, ET C'EST LA MOITIÉ FAUSSE QUI COÛTAIT.** Le script disait : un rapport calculé contre la couleur de REPLI serait une fiction. C'est vrai. Il en concluait qu'on ne peut rien calculer, et c'est faux : **les ARRÊTS du dégradé bornent le vrai rapport où que le texte tombe dessus**. Trois issues, dont deux sont des verdicts nets — passe au pire arrêt → mesure définitive ; échoue au meilleur → défaut réel ; entre les deux → dépend vraiment de l'endroit, signalé avec ses DEUX bornes et **jamais** mis en échec, parce qu'une borne basse n'est pas une mesure et qu'une porte trop stricte fait supprimer du travail correct. Garde-fous : un seul calque (deux `gradient(` = superposition, on ne sait pas lequel peint) et des arrêts tous opaques ; sinon on saute ET on dit pourquoi. **RÉSULTAT : 59 sautés → 0, et 2593 → 2652 mesurés**, soit exactement 2593 + 59. Plus une seule omission dans la campagne.

**UN CAS DIT TOUT DE CE QUE LE MOT CACHAIT** : en mode clair, le « dégradé » de la carte IA est `rgb(242,236,219) → rgb(242,236,219)` — **deux arrêts identiques**. Cet élément était parfaitement mesurable depuis toujours ; il était sauté parce que la présence du mot `linear-gradient` suffisait à fermer les yeux.

**DÉFAUT 1 — LA NOTICE QUI DIT COMMENT RÉACTIVER L'IA ÉTAIT LE TEXTE LE MOINS LISIBLE DE LA CARTE.** Le conteneur d'`AICard` portait `opacity: hasKey ? 1 : 0.7`, qui éteignait TOUT son contenu, y compris `ai_no_key_hint` — « Ajoute une clé API dans Paramètres → IA ». **3,15:1 pour 4,5 requis** en sombre, sur les six palettes et cinq écrans de formulaire ; « Auto-compléter » à **3,31-3,34:1** en clair. **Rien ne se perd du signal « inactif », parce qu'il n'a jamais été porté par cette ligne** : les deux commandes déclarent `aria-disabled`, portent leur propre `opacity: 0.6` et un curseur `not-allowed` — WCAG 1.4.3 exempte les composants INACTIFS, et elles seules le sont. La bordure de la carte s'assourdit déjà. Ce 0.7 se multipliait d'ailleurs avec le 0.6 des boutons (0,42), ce que la doc décrivait comme « 0.7 × 0.6 » **sans relever qu'il s'appliquait aussi au texte d'information**. *Un état désactivé se signale sur la COMMANDE, pas sur la notice qui explique comment en sortir.*

**DÉFAUT 2 — LE CTA S'EXEMPTAIT D'UNE RÈGLE QUI ÉTAIT LA SIENNE.** Le jeton portait le commentaire *« un filled focal a son propre contraste, pas la règle du texte »* — alors que ce bouton PORTE un titre et une sous-ligne. Mesuré : le crème donnait **4,83:1 sur l'arrêt oxblood et 2,76:1 sur l'ember**, la sous-ligne **2,39:1**. Le titre passait donc sur une moitié du bouton et échouait sur l'autre. **AUCUNE ENCRE NE LE SAUVAIT**, et c'est ce qui a décidé de la forme du correctif : luminance du crème 0,816, de l'ember 0,264, et il faut un fond ≤ 0,143 pour 4,5:1 — **un blanc pur plafonnait à 3,35:1**. Le problème était le FOND. Décision de l'utilisateur, ×0,75 sur les deux arrêts (`#a8453f → #7e342f`, `#df6a40 → #a75030`) : **7,16:1 et 4,52:1**, conforme sur toute la longueur, teinte et direction conservées.

**ET LA SOUS-LIGNE A REJOUÉ UN MOTIF DÉJÀ PAYÉ ICI.** Elle restait encadrée 3,73-5,65:1 quand le titre, MÊME COULEUR SUR LE MÊME DÉGRADÉ, passait à 4,52-7,16:1 — la seule différence était un `opacity: 0.85` de mise en retrait typographique. C'est exactement ce que ce script avait déjà fait payer en resserrant son exemption `dimmed` : *« une opacité réduite n'est pas une preuve de désactivation »*, les badges POT / CAVE portaient 0.75 pour la même raison. Retirée : la hiérarchie est déjà portée par la taille (13,5 contre 18), la graisse et l'italique.

**CE QUI EST VERROUILLÉ, ET POURQUOI SOUS CETTE FORME.** (1) Le CTA est gardé **par le calcul** — le test recalcule le rapport WCAG des jetons au lieu d'épingler `"#7e342f"`, sinon on figerait une couleur tout en laissant repasser le défaut à la première retouche ; il rougit en NOMMANT la valeur (`#df6a40 : 2.76:1`). (2) L'encadrement est gardé sur sa **décision à trois branches**, pas sur son orthographe : retirer celle du milieu ferait échouer sur une borne basse, retirer les deux autres ramènerait l'abandon. (3) La carte IA est gardée **des deux côtés** — conteneur non assombri ET commandes toujours `aria-disabled` + `not-allowed` —, car ne tenir que la première moitié rendrait acceptable une carte qui ne signale plus rien. Sondées une à une, rouges à chaque fois, fichiers restaurés à l'empreinte près.

**DEUX ERREURS DE MÉTHODE DANS LA SONDE ELLE-MÊME, du type exact qu'elle cherchait.** (a) Les six palettes tournent en shards parallèles ; elles héritaient toutes de `THEME_CONTRAST_SKIPPED` et **écrivaient dans le même chemin** — le dernier aurait gagné et j'aurais présenté un sixième des lignes comme « les 59 ». Un fichier par shard. (b) `pkill -f "theme-contrast"` a **tué mon propre shell**, dont la ligne de commande contient ce motif, une heure après que la même chose ait été inscrite ici pour `vite preview` : le `python3` qui suivait n'a jamais tourné, et seule la vérification (0 occurrence du nouveau commentaire) a empêché d'enchaîner en croyant le contraire. **Une sonde dont on n'a pas vérifié qu'elle s'est APPLIQUÉE ne dit rien** — y compris quand elle est verte : une troisième sonde est revenue verte sur une substitution `perl` qui n'avait rien remplacé.

## `i18n:layout` affirmait un négatif sans dénominateur — et son angle mort était sa propre récidive

**LA PREMIÈRE MESURE N'A DEMANDÉ AUCUNE SONDE : le script concluait « no overflow, no clipped text, no stray horizontal scroller, nothing cut off » sans jamais dire sur COMBIEN d'éléments.** Une affirmation purement négative sans portée se lit comme une couverture ; elle n'en est pas une. C'est le défaut que ce fichier avait déjà nommé pour `theme:contrast`, **en pire** : là il manquait le total des OMISSIONS, ici il manquait aussi celui des MESURES. La garde de non-vacuité existante (`totalCards`, qui tue la campagne à zéro carte) vérifie que la GRAINE s'est chargée — pas que les règles ont examiné quoi que ce soit.

**L'ANGLE MORT EST LA RÉCIDIVE EXACTE DU DÉFAUT QUI AVAIT MOTIVÉ LA QUATRIÈME RÈGLE.** Le cas « BEARBE » (trois boutons peints hors d'un panneau `overflow: hidden`, invisibles aux trois premières règles) a produit une quatrième règle — restée `leaves only`. Or « feuilles seulement » est JUSTE quand le texte vit dans un enfant et AVEUGLE quand il vit directement dans le nœud. **MESURÉ : 13 792 éléments, soit 18,9 % de la population porteuse de texte, qu'aucune des quatre règles ne regardait** — dont un écran (`help`) comptant **296 non examinés pour 193 examinés**.

**J'AVAIS ILLUSTRÉ CE TROU AVEC LES PUCES DE FILTRE, ET L'ILLUSTRATION ÉTAIT FAUSSE — écrite, comme d'habitude, sans mesure.** J'avais annoncé « Tous / En cave / jeune 2 … le genre de contrôle serré qui se coupe en allemand », puis, dans la liste des suites, « peu de marge, un libellé plus long les mettrait dehors ». Les deux sont une DÉDUCTION tirée de leur présence parmi les 13 792 — or y figurer signifie seulement « du texte propre PLUS des enfants », et ne dit rien d'une étroitesse. **MESURÉ ensuite, en allemand à grande taille sur 360 px : la rangée porte `data-hscroll` et `overflowX: auto`, elle déborde DÉJÀ de 554 px et défile ; les sept puces sont dimensionnées à leur contenu (75 à 176 px) ; aucune ne coupe son propre texte.** Un libellé plus long fait défiler un peu plus loin, rien d'autre — et la quatrième règle les blanchit de toute façon au `data-hscroll`, donc elles n'étaient même pas concernées par l'angle mort. **Déduire un risque de l'appartenance à une population est exactement l'erreur que ce vérificateur existe pour ne plus commettre, et je l'ai commise en rédigeant la liste des suites.** Le trou et sa démonstration tiennent sans cette illustration : ils reposent sur le défaut INJECTÉ, invisible avant, nommé après.

**ZÉRO COUPE RÉELLE, ET C'EST CE QUI REND L'AJOUT GRATUIT PLUTÔT QU'INUTILE.** Le point qui autorise à écrire ce zéro est la NON-VACUITÉ du détecteur : seuil abaissé, **489 détections sur `fr/m/360` seul**. Un détecteur qui rend 0 doit d'abord prouver qu'il sait rendre ≥ 1 — cette session avait déjà rencontré trois faux verts de cette forme. **La démonstration décisive est un défaut INJECTÉ** dans un nœud mixte (texte propre débordant de 257 px un ancêtre de 40 px) : **règle d'origine → exit 0, aucune mention ; règle étendue → exit 1, en nommant le texte et le débordement.** Le même contrôle illisible, et la campagne passait au vert.

**LA MESURE SE FAIT PAR `Range`, PAS PAR LE RECTANGLE DE L'ÉLÉMENT** — celui-ci engloberait ses enfants, donc il répondrait à une autre question que celle posée. Un `Range` par nœud de texte direct, bord droit maximal, puis exactement le même juge que pour une feuille : `clipAncestorOf` et `reportIfCut` ont été EXTRAITS pour cela, parce que deux copies de la marche vers l'ancêtre coupant sont la classe de dérive que ce dépôt a déjà payée six fois.

**DEUX ERREURS DE DÉCOUPAGE DANS MES PROPRES GARDES, LA MÊME À UNE HEURE D'INTERVALLE.** Pour `theme:contrast`, une regex non gourmande sur `}` s'arrêtait à l'accolade d'un `${theme}` **à l'intérieur** d'un littéral de gabarit, et la branche paraissait vide. Ici, `indexOf("continue;")` tombait sur le `continue` **de la boucle interne** sur les nœuds enfants, et rendait une tranche tronquée. **Un délimiteur qui peut apparaître à l'intérieur de ce qu'on découpe n'est pas une borne** — borner sur ce qui SUIT, et vérifier que la borne a été trouvée.

## Trois suites prises d'un coup, et l'une d'elles m'a démenti

**(1) LA PORTE DE BUMP NE VOYAIT PAS L'ARBRE DE TRAVAIL, et le défaut était SYMÉTRIQUE.** Elle comparait `git diff <dernier-bump> HEAD`, c'est-à-dire deux COMMITS. La moitié visible était un faux rouge — un bump écrit mais non commité lui échappait, et le rouge se résolvait seul au commit suivant : inoffensif, déroutant, et **c'est le genre de bruit qui apprend à ignorer une porte**. La moitié cachée est la grave : **une vue modifiée et non commitée lui échappait aussi**, donc elle n'avertissait qu'APRÈS le commit, c'est-à-dire trop tard pour servir. Omettre `HEAD` compare l'arbre au commit de bump et couvre les deux ; les fichiers NON SUIVIS sont ajoutés à part, `git diff` ne les voyant pas du tout — un fichier neuf sous `src/views/` est exactement ce que cette porte existe pour attraper. Sondé dans les deux sens. **Le second essai a d'abord rougi pour une AUTRE raison** (`version.json` resté à l'ancien numéro, une porte différente qui avait raison) : vérifier LAQUELLE a tiré avant de conclure, sinon on attribue un rouge au mauvais gardien.

**(2) « LES PUCES DE FILTRE SONT SERRÉES » ÉTAIT FAUX, ET JE L'AVAIS ÉCRIT SANS MESURER.** J'avais annoncé, dans la liste des suites, « peu de marge — un libellé plus long les mettrait dehors », en le DÉDUISANT de leur présence parmi les 13 792 éléments non examinés. Or y figurer signifie seulement « du texte propre PLUS des enfants », et ne dit rien d'une étroitesse. **MESURÉ en allemand à grande taille sur 360 px : la rangée porte `data-hscroll` et `overflowX: auto`, elle déborde DÉJÀ de 554 px et défile ; les sept puces sont dimensionnées à leur contenu (75 à 176 px) ; aucune ne coupe son propre texte.** Un libellé plus long fait défiler un peu plus loin, rien d'autre — et la quatrième règle les blanchit de toute façon au `data-hscroll`, donc elles n'étaient même pas concernées par l'angle mort qu'elles étaient censées illustrer. **Déduire un risque de l'appartenance à une population est exactement l'erreur que ces vérificateurs existent pour ne plus commettre, et je l'ai commise en rédigeant la liste des suites d'un travail qui portait là-dessus.** Rétracté aux deux endroits où je l'avais écrit.

**(3) LE BALAYAGE DES GARDES QUI ÉPINGLENT L'ORTHOGRAPHE — 16 trouvées, 7 corrigées, 9 laissées, et le tri est tout l'intérêt.** Cherchées par leur SIGNATURE plutôt qu'à la main : une assertion sur du source dont l'argument contient une AFFECTATION nue (` = ` qui n'est ni `===`, ni `=>`, ni `<=`). Les neuf laissées épinglent une chose qui COMPTE — le nom d'un helper extrait (c'est l'extraction qu'on affirme), `const` là où l'immuabilité est la propriété, `new Map` là où la structure est la propriété, et une assertion NÉGATIVE (y figer l'ancienne forme est correct : elle ne doit pas revenir). Les sept corrigées figeaient un mot-clé `var`, un point-virgule ou un ordre d'opérandes commutatif — un changement sémantiquement neutre les faisait rougir pour rien.

**ET LA PARTIE QUI COMPTE EST CELLE D'APRÈS : une garde assouplie qui ne garde plus rien serait pire que la fragilité ôtée.** Chacune des sept a été éprouvée en CASSANT sa propriété dans le source — capture de session supprimée, `"pseudo"` retiré du prédicat, `++` retiré de la génération, `ENTER_MAX_DELAY_MS` sorti du `Math.min`, l'offset mesuré remplacé par 0, la cible de l'IA figée sur une seule branche, `.map` changé en `.filter` — **sept rouges sur sept**, fichiers restaurés à l'empreinte près. **Le sens de l'erreur n'est pas symétrique et c'est ce qui justifie de n'en corriger que sept sur seize** : une garde trop stricte coûte deux minutes et force une relecture ; une garde creuse est silencieuse et laisse passer le défaut. Dans le doute, on garde la rigidité.

## Les deux vérificateurs restants portaient le même défaut, et l'un passait à VIDE

**LA PISTE ÉTAIT « LES AUTRES VÉRIFICATEURS N'ONT JAMAIS ÉTÉ AUDITÉS POUR CE DÉFAUT » — annoncer un résultat sans annoncer sa portée.** Quatre vérificateurs maison : `theme:contrast` et `i18n:layout` traités le jour même, restaient `size:check` et `doc:check`.

**`size:check` PASSAIT AU VERT EN AYANT PESÉ ZÉRO OCTET, et ce n'est pas une hypothèse.** Le jeu des scripts chargés d'emblée vient d'un regex sur le HTML bâti. **MESURÉ en cassant le motif** (`/assets/` → un nom qui ne correspond à rien) : la porte imprime `[ok ] eager scripts (home) 0.0 KB / 332.0 KB`, puis `check-bundle-size OK`, et sort **0**. Le jour où Vite change la forme de ses chemins — un sous-dossier, une base différente — le budget devient vert pour toujours. **Un budget respecté parce qu'on n'a rien pesé n'est pas un budget respecté**, et rien dans la sortie ne le distinguait d'un vrai vert : la même ligne, le même mot, le même code de sortie.

**ET L'OMISSION SILENCIEUSE RENDAIT LA PORTE PLUS FACILE À PASSER, ce qui est le pire sens.** `if (!fs.existsSync(p)) continue;` retirait du total le poids d'un fichier référencé mais introuvable. Un `continue` muet qui allège un budget est une porte qui se desserre toute seule — et c'est en outre un défaut à part entière, puisque le navigateur, lui, demandera ce fichier et recevra un 404. Les deux cas refusent désormais, en nommant ce qui manque ; et le total dit **sur combien de fichiers il porte** (9 aujourd'hui, 282,3 Ko).

**`doc:check` CONFONDAIT « PAS ENCORE TRADUIT » ET « LE WRAPPER A DISPARU ».** `checkHelpAnchors` sautait en silence toute langue sans `<div id="sec-XX">`, au nom d'une traduction à venir — alors que cette porte existe **précisément** pour le cas où une balise malformée AUTO-FERME ce wrapper. Un wrapper entièrement absent en est l'extrême, et la porte se taisait au lieu de crier plus fort. **Le départ se lit sur les ANCRES** : si les h2 de cette langue existent encore dans le document, la langue EST traduite et c'est son wrapper qui manque. **MESURÉ sur le vrai `help.html` en retirant `sec-pt` : avant, exit 0 sans un mot ; après, la portée tombe de 108 à 90 ancres vérifiées et la porte nomme les 18 orphelines.**

**LA PORTÉE EST DÉSORMAIS IMPRIMÉE À CÔTÉ DU VERDICT** — « 108 ancre(s) vérifiée(s), toutes langues présentes ». Sans elle, `doc:check OK` couvrait aussi bien six langues validées qu'une seule. C'est le troisième vérificateur sur quatre à porter ce défaut, et la forme ne varie pas : **le chiffre manquant existait toujours, et son absence ne se voyait jamais.**

## Les 17 entrées du changelog, relues une à une contre le code

**L'UTILISATEUR A CONTESTÉ LE DIAGNOSTIC, ET IL AVAIT RAISON DE LE FAIRE :** « tout le changelog a été refait intégralement il y a quelques jours ». J'avais parlé d'obsolescence. La chronologie dit autre chose. **Le 25 août, la pierre tombale de `CuratorDelConfirmModal` est DÉJÀ dans le premier commit visible ; le 28 août, l'entrée Build 0 écrit « les gestes qui effacent, qui doivent MAINTENANT demander avant » ; et au même commit, le bouton corbeille appelle `deleteTobacco(tob.id)` avec ZÉRO `window.confirm` dans la vue.** La phrase n'a pas vieilli — **elle était fausse le jour de son écriture**, et le mot « maintenant » annonçait comme nouveau un comportement supprimé trois jours plus tôt. Ce n'est pas un problème de temps mais de MOMENT : une entrée rédigée d'après une idée de ce que contenait la version, pas d'après le code.

**ET LA REFONTE ÉVOQUÉE N'AURAIT RIEN PU Y FAIRE.** Dans l'historique visible, le changelog n'a jamais été réécrit en bloc : il existe complet au premier commit (391 lignes) et s'allonge depuis, **18 commits, une entrée par build**. La purge des ~40 puces sur le catalogue que `CLAUDE.md` décrit est antérieure au 25 août — donc antérieure à l'entrée fautive. **Une refonte ne corrige pas une entrée qui n'existe pas encore.**

**RELECTURE DES 17 ENTRÉES : SEIZE EXACTES, UNE FAUSSE** (celle-là, corrigée). Chacune vérifiée par ce qui la rend vraie, pas par sa vraisemblance — `cave-theme` pour les trois palettes, `cave-font-scale` pour la taille de texte, `home_pair_accord` pour la pipe accordée, `autosaveFailKey` pour les échecs de sauvegarde, `xlValue(category, CATS_EN, lang)` + `t("prio_high")` pour les valeurs CSV traduites, `setSelectedSet(importAllKeys(parsed))` pour le sélecteur pré-coché, `window.confirm(import_replace_confirm)` pour le mode « Remplacer », et les tests NOMMÉS pour Dropbox et les quatre blocs CSV. **Un cas méritait d'être regardé deux fois** : `pool = saturated > 0 ? saturated : Math.min(5, …)` — le plafond de cinq pipes existe encore, mais seulement en REPLI quand aucune n'a assez reposé, donc l'entrée (« toutes celles qui ont assez reposé ») dit exactement le vrai. Lire le plafond sans lire sa condition aurait produit un faux positif.

**TROIS GREPS À MOI SONT REVENUS À ZÉRO SANS QUE LA FONCTIONNALITÉ MANQUE** — palettes, taille de texte, mention « accordée ». Le bon terme était ailleurs (`cave-theme`, `cave-font-scale`, `home_pair_accord`). **Un grep qui ne trouve rien ne prouve rien** : c'est la sonde non appliquée, sous une autre forme, et elle est plus dangereuse ici parce qu'un zéro ressemble à une réponse.

**ET LA MÊME MALADIE DANS `CLAUDE.md`, TROUVÉE EN TIRANT CE FIL.** La section changelog affirme « *Verified afterwards: zero lines matching `catalog|catálog|katalog` in any section* ». **MESURÉ : 15, toutes dans les sections.** Relues une à une : ce sont des mentions incidentes légitimes — un LIEU de l'app, un BOUTON, un VOLUME d'exemple — donc **la règle tient, c'est la ligne de vérification qui a pourri**. Une mesure datée, écrite au présent dans une section de règles, se relit comme un état courant. Troisième instance de cette forme dans la même journée, après les 59 éléments « restant » sautés et la réinstallation « requise ».

**CE QUI RESTE OUVERT, ÉNONCÉ ET NON CACHÉ :** rien ne garde le changelog contre une entrée fausse. Ses portes vérifient la structure, la parité des six langues, le numéro de build et le vocabulaire — aucune ne peut vérifier qu'une phrase décrit l'application, parce que c'est de la prose. La seule méthode connue est celle qui vient d'être employée : relire chaque affirmation contre le code. Elle a coûté une heure pour 17 entrées et elle en a trouvé une.
