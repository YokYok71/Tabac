{
  "name": "cave-tabac",
  "version": "1.0.0",
  "description": "Ma Cave à Tabac — Application iOS via Capacitor",
  "scripts": {
    "prepare": "mkdir -p www && cp index.html manifest.json sw.js reset.html icon-192.png icon-512.png www/",
    "sync": "npm run prepare && npx cap sync ios",
    "open": "npx cap open ios",
    "ios": "npm run sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/keyboard": "^6.0.0",
    "@capacitor/share": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.0"
  }
}
